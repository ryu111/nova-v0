# 知識治理與快照 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 【推論】建立跨 Work 的知識治理權威：執行者只能提案，唯一 admission writer 決定 ACTIVE／過期／撤銷／取代；Work 只綁 immutable KnowledgeSnapshot，來源失效會沿完整衍生圖傳播而不改寫歷史。

**Architecture:** 【推論】KnowledgeAssertion 的內容 bytes 在 CAS、provenance/derivation 與 lifecycle events 在權威 state store、查詢 index 只是可重建 projection。Knowledge Authority 先依來源、範圍、TTL、admission rule 建立 assertion revision，再將同一次 deterministic resolution 固化成 snapshot。`ADVISORY_CONSTRAINT` 是 assertion profile，與一般知識共用同一支 admission/revocation 筆；它的 ConstraintSpec parsing、scope packing與 context injection 留給 plan 12，避免共用 parser取得知識寫權。

**Tech Stack:** 【推論】CPython 3.14.7、MachineSpec、CAS、SQLite recursive CTE/adjacency index、canonical JSON、fake clock、ClaimSpec runner。

**Spec:** 【查證】本檔「子系統規格」，以及[第二輪知識權威裁決](../sol-新局-第二輪.md#1-q1這個對應缺一面知識治理)、[第四輪告知型約束歸屬](../sol-新局-第四輪.md#12-跨工作的告知型約束確實歸知識不是方便塞法)。

## Global Constraints

- 【推論】Assertion status 固定 `PROPOSED|ACTIVE|REVIEW_REQUIRED|EXPIRED|REVOKED|SUPERSEDED|REJECTED`；內容 revision immutable，狀態不寫回 source bytes。
- 【推論】只有 Knowledge Authority 可進 `ACTIVE` 或撤銷/取代；agent、Work、Pursuit、health observer、HTTP/CLI caller 都只能 `PROPOSED`。
- 【推論】來源不是 URL string；至少有 source entity、generation activity、responsible agent、derivation edges、revision/invalidation evidence refs。
- 【推論】預設 TTL 90 天；可縮短，延長必須新 revision 重跑 admission。沒有永不過期值。
- 【推論】未 ACTIVE 的 assertion 永遠不進 snapshot；snapshot digest 在時間推進、TTL 到期或 index rebuild 後解析相同 revision set。
- 【推論】撤銷保存歷史使用痕跡；新 Work 取不到 revoked assertion，在途 Work 收到 `KNOWLEDGE_REVOKED` 並按釘版 risk policy 顯式 rebase/taint/cancel。
- 【推論】知識不能直接改判準、加預算或產生外部 effect intent；只能透過窄 proposal/snapshot contract被其他權威採用。

## 子系統規格

【推論】`KnowledgeAssertion` 必填 `assertion_id/revision/profile/body_ref/scope/source_evidence_refs/derived_from/proposed_by/admission_rule_ref/valid_from/expires_at/supersedes`；`profile` v1 固定 `GENERAL|ADVISORY_CONSTRAINT`。

【推論】`KnowledgeSnapshot` 固定包含 ordered `(assertion_id, revision, digest)`、`resolved_at`、authority event cursor、query/input facts digest、query policy digest、backend fingerprint 與 serialization-policy digest。相同 authority event prefix、query facts、policy revision、clock instant 與 backend fingerprint 必須解析出同一 snapshot digest；把一次結果存下來只能證明 load 可重播，不能替代 resolve 的決定性。

【推論】v1 query policy 固定六步，沒有 ranked retrieval：①在輸入 event cursor fold exact authority state；②以 typed scope 做 exact filter；③驗證 `ACTIVE`、TTL 與 profile；④沿 provenance DAG 取 deterministic closure（符合者加所有 ACTIVE ancestors；失效 ancestor 只保留 evidence ref，不注入內容）；⑤stable topological sort，ancestor 先於 descendant，同一 topology layer 依 scope 由廣到窄，再以 `(assertion_id, revision, digest)` tie-break；⑥把全部 eligible assertion canonicalize 成 context 與 snapshot，不做 top-k、entity extraction、摘要樹或語義裁剪。

【推論】知識 context hard budget 為 `min(32_768 個 backend-measured input tokens, backend usable context 扣除 system/tool/output 等 mandatory reserves 後的 25%)`。全部 eligible bytes 放不下就回 typed `KNOWLEDGE_CONTEXT_OVERFLOW`；不得截尾、摘要、改用相似度，也不得把成功包裝成「部分知識」。

【推論】KV／prompt cache 只能加速 exact payload：cache key 至少含 snapshot digest、backend/model exact fingerprint 與 serialization-policy digest；hit/miss 送出的 canonical bytes 必須逐 byte 相同，cache 不得選候選、改順序或摘要。

【推論】來源撤銷以 transitive closure 找所有 descendant；每個 descendant 至少進 `REVIEW_REQUIRED`，只有 admission rule 證明仍有足夠獨立來源才可用 successor revision 恢復 ACTIVE，同一 revision 不原地洗白。

## File Structure

```text
規格/知識/
├── 知識.machine.json                         — assertion admission/review/revoke lifecycle。
├── KnowledgeAssertion.schema.json            — content/provenance/scope/TTL/profile。
├── KnowledgeSnapshot.schema.json             — immutable ordered resolved set。
├── RetrievalSelectionPolicy.schema.json       — 未來 retrieval 的量測門檻與 admission evidence。
├── 初始知識政策.policy.json                  — 90-day TTL/query/risk defaults。
└── 保證/
    ├── 未准入不可取用.claim.json             — PROPOSED never enters snapshot。
    ├── 執行者只能提案.claim.json             — caller status ACTIVE direct red。
    ├── 撤銷傳遞.claim.json                   — second-order derived assertion tainted。
    ├── 快照可重播.claim.json                 — time/index changes cannot drift snapshot。
    ├── 範圍圖全量決定性.claim.json           — exact scope→DAG closure→stable topo→full eligible。
    ├── 索引重建語義同一.claim.json           — rebuild 不得改 semantic selection。
    ├── 知識上下文不得靜默截斷.claim.json     — overflow typed failure，不可 partial success。
    ├── 快取只准加速.claim.json               — cache hit/miss outbound bytes identical。
    └── 知識不得越權.claim.json               — no criterion/budget/effect write port。
nova/權威/知識/
├── 模型.py                                   — assertion/snapshot/provenance immutable types。
├── 准入.py                                   — proposal and authority-only activation decisions。
├── 來源圖.py                                 — derivation graph and invalidation closure。
├── 檢索.py                                   — ACTIVE-only deterministic query planning。
├── 快照.py                                   — resolution to immutable snapshot/CAS bytes。
├── 撤銷.py                                   — source/assertion invalidation propagation。
├── 端口.py                                   — content/index/event/approval protocols。
└── test_知識權威.py                          — lifecycle/provenance/snapshot tests。
nova/基礎設施/知識索引/
├── sqlite.py                                 — rebuildable scope/text metadata index。
├── 重建.py                                   — event/CAS projection rebuild。
└── test_索引.py                              — index loss/rebuild parity。
nova/應用/
├── 提案知識.py                               — untrusted proposal command handler。
├── 審核知識.py                               — authority admission/revision handler。
├── 撤銷知識.py                               — source/assertion invalidation command。
├── 建立知識快照.py                           — query facts to pinned snapshot。
└── test_知識服務.py                          — ACL/idempotency/rebase signal tests。
驗收/知識/
├── 測_准入邊界.py                            — agent cannot self-admit。
├── 測_撤銷傳遞.py                            — deep/wide derivation closures。
├── 測_快照重播.py                            — clock/index/restart stability。
├── 測_決定性檢索政策.py                      — 六步 policy、topological tie-break、overflow/cache negatives。
├── 測_retrieval反轉門檻.py                   — 30-day trigger 與五項 adoption gates。
└── 測_跨權威越界.py                          — knowledge has no direct criterion/resource/effect ports。
```

## Dependency Gate

前置計畫：01 02 03 04 09

【推論】必須先完成 plan 01–04 與 09；不直接依賴 plan 12。KnowledgeSnapshot 必須由 CAS/event/state owner 重建，且 Work 要有 pinned-ref 欄位與 invalidation signal 入口。若先做 prompt 注入再做本 plan，檢索結果會變成 live mutable memory，重播時無法知道當時到底注入哪版。

---

### Task 1: 宣告 KnowledgeAssertion machine 與封閉 schema

**Files:**
- Create: `規格/知識/知識.machine.json`
- Create: `規格/知識/KnowledgeAssertion.schema.json`
- Create: `規格/知識/初始知識政策.policy.json`
- Create: `nova/權威/知識/模型.py`
- Create: `nova/權威/知識/准入.py`
- Create: `nova/權威/知識/端口.py`
- Create: `nova/權威/知識/test_知識權威.py`

**Interfaces:**
- Produces: `propose_assertion(payload) -> KnowledgeProposal`。
- Produces: `admit_assertion(proposal_ref, admission_evidence, authority) -> AssertionActivated|Rejected`。

**ClaimSpec:** 【推論】`knowledge.lifecycle.declared-and-versioned` 從紅轉綠。

**固定負控:** 【推論】同 revision `REVIEW_REQUIRED -> ACTIVE`、`REVOKED -> ACTIVE` 或 source bytes 自帶 `status=ACTIVE`；MachineSpec/schema 必須拒絕。

- [ ] **Step 1: 寫非法 reactivation 與 unknown status field red**

```python
def test_assertion_source_cannot_claim_active() -> None:
    payload = minimal_assertion_source() | {"status": "ACTIVE"}
    assert validate_assertion_source(payload).code == "UNKNOWN_FIELD"
```

- [ ] **Step 2: 跑測試確認 schema/machine 缺失**

Run: `uv run pytest -q nova/權威/知識/test_知識權威.py -k lifecycle`

Expected: 【推論】FAIL。

- [ ] **Step 3: 寫 exact states、profile union 與 90-day default policy**

```python
class AssertionProfile(StrEnum):
    GENERAL = "GENERAL"
    ADVISORY_CONSTRAINT = "ADVISORY_CONSTRAINT"
```

- [ ] **Step 4: 跑 schema/machine tests 與 ClaimSpec**

Run: `uv run pytest -q nova/權威/知識/test_知識權威.py 驗收/狀態機/測_lint.py && uv run python 工具/跑驗收.py --claim knowledge.lifecycle.declared-and-versioned`

Expected: 【推論】PASS；三個 illegal transitions direct red。

- [ ] **Step 5: Commit**

```bash
git add 規格/知識/知識.machine.json 規格/知識/KnowledgeAssertion.schema.json 規格/知識/初始知識政策.policy.json nova/權威/知識
git commit -m "feat: declare governed knowledge assertions"
```

---

### Task 2: 封死執行者自我准入

**Files:**
- Create: `nova/應用/提案知識.py`
- Create: `nova/應用/審核知識.py`
- Create: `nova/應用/test_知識服務.py`
- Create: `驗收/知識/測_准入邊界.py`
- Create: `規格/知識/保證/執行者只能提案.claim.json`

**Interfaces:**
- Produces: untrusted `propose` command with no status/admitted_by fields。
- Consumes: `KnowledgeApproverCapability` only on trusted review route。

**ClaimSpec:** 【推論】`knowledge.admission.executor-proposal-only` 從紅轉綠。

**固定負控:** 【推論】fake executor 傳 `desired_status=ACTIVE`、偽造 approver id 或直接呼叫 repository；只能得到 `PROPOSED`/authorization error，無 ACTIVE event。

- [ ] **Step 1: 寫 public API/ACL red**

```python
def test_executor_cannot_mint_active_assertion() -> None:
    result = public_knowledge_api.propose(caller=executor_identity(), body_ref=fixed_body_ref())
    assert result.status is AssertionStatus.PROPOSED
    assert not event_log.contains("AssertionActivated")
```

- [ ] **Step 2: 跑測試確認 handler 可能接受 caller status**

Run: `uv run pytest -q 驗收/知識/測_准入邊界.py`

Expected: 【推論】FAIL。

- [ ] **Step 3: 寫分開的 command types/ports；repository 不暴露給 adapters**

```python
@dataclass(frozen=True, slots=True)
class ProposeKnowledge:
    body_ref: ContentRef
    provenance: ProvenanceInput
    profile: AssertionProfile
```

- [ ] **Step 4: 跑 API/architecture/ClaimSpec**

Run: `uv run pytest -q nova/應用/test_知識服務.py 驗收/知識/測_准入邊界.py 驗收/架構/測_import規則.py && uv run python 工具/跑驗收.py --claim knowledge.admission.executor-proposal-only`

Expected: 【推論】PASS；fake status/approver/repository negatives direct red。

- [ ] **Step 5: Commit**

```bash
git add nova/應用/提案知識.py nova/應用/審核知識.py nova/應用/test_知識服務.py 驗收/知識/測_准入邊界.py 規格/知識/保證/執行者只能提案.claim.json
git commit -m "feat: restrict executors to knowledge proposals"
```

---

### Task 3: 建立可追溯的來源與衍生圖

**Files:**
- Create: `nova/權威/知識/來源圖.py`
- Modify: `nova/權威/知識/模型.py`
- Modify: `nova/權威/知識/准入.py`
- Modify: `nova/權威/知識/test_知識權威.py`

**Interfaces:**
- Produces: `ProvenanceGraph(entity/activity/agent/derivation/invalidation refs)`。
- Produces: cycle-free `derived_from_assertion_ids` adjacency。

**ClaimSpec:** 【推論】`knowledge.provenance.structured-and-acyclic` 從紅轉綠。

**固定負控:** 【推論】只有自由 URL 字串、缺 responsible agent/activity，或 A derived-from B derived-from A；admission 必須 direct red。

- [ ] **Step 1: 寫 required provenance/cycle red**

```python
def test_derivation_cycle_is_rejected() -> None:
    graph = graph_with_edges(("a", "b"), ("b", "a"))
    assert validate_derivation(graph).code == "DERIVATION_CYCLE"
```

- [ ] **Step 2: 跑測試確認 URL-only 被接受**

Run: `uv run pytest -q nova/權威/知識/test_知識權威.py -k provenance`

Expected: 【推論】FAIL。

- [ ] **Step 3: 寫 typed provenance refs 與 DAG validation**

```python
@dataclass(frozen=True, slots=True)
class Provenance:
    source_entities: tuple[EvidenceRef, ...]
    generation_activity_ref: EvidenceRef
    responsible_agent_ref: IdentityRef
    derived_from: tuple[AssertionRef, ...]
```

- [ ] **Step 4: 跑 property tests 與 ClaimSpec**

Run: `uv run pytest -q nova/權威/知識/test_知識權威.py && uv run python 工具/跑驗收.py --claim knowledge.provenance.structured-and-acyclic`

Expected: 【推論】PASS；任意 cycle 被拒、topological order deterministic。

- [ ] **Step 5: Commit**

```bash
git add nova/權威/知識/來源圖.py nova/權威/知識/模型.py nova/權威/知識/准入.py nova/權威/知識/test_知識權威.py
git commit -m "feat: record structured knowledge provenance"
```

---

### Task 4: 只從 ACTIVE assertions 建立 immutable snapshot

**Files:**
- Create: `規格/知識/KnowledgeSnapshot.schema.json`
- Create: `nova/權威/知識/檢索.py`
- Create: `nova/權威/知識/快照.py`
- Create: `nova/應用/建立知識快照.py`
- Create: `驗收/知識/測_快照重播.py`
- Create: `規格/知識/保證/未准入不可取用.claim.json`
- Create: `規格/知識/保證/快照可重播.claim.json`

**Interfaces:**
- Produces: `resolve_snapshot(query_facts, policy_ref, now) -> KnowledgeSnapshotRef`。
- Produces: `load_snapshot(ref) -> exact ordered AssertionRef tuple` without live query。

**ClaimSpec:** 【推論】`knowledge.snapshot.active-only` 與 `knowledge.snapshot.replay-stable` 從紅轉綠。

**固定負控:** 【推論】拿掉 `status=ACTIVE` filter，或 load snapshot 時重新執行 current query；PROPOSED 混入與 TTL/index rebuild 漂移都要 direct red。本 task 先建立 immutable snapshot；完整六步 resolution 與 stable topological order 在 Task 8 收口。

- [ ] **Step 1: 寫 mixed-status selection 與 clock-advance red**

```python
def test_snapshot_load_does_not_live_requery() -> None:
    ref = create_snapshot(at=t(0), active=("a@1",))
    expire("a@1", at=t(1))
    assert load_snapshot(ref).assertion_refs == (ref_of("a@1"),)
```

- [ ] **Step 2: 跑 tests 確認 live query drift**

Run: `uv run pytest -q 驗收/知識/測_快照重播.py`

Expected: 【推論】FAIL。

- [ ] **Step 3: 寫 deterministic sort/canonical bytes/CAS put**

```python
refs = tuple(stable_topological_order(active_closure, tie_break=("scope_specificity", "assertion_id", "revision", "digest")))
snapshot_ref = content_store.put(canonical_json_bytes(KnowledgeSnapshot(refs=refs, resolved_at=now, query_digest=query_digest)))
```

- [ ] **Step 4: 跑兩份 ClaimSpec**

Run: `uv run python 工具/跑驗收.py --claim knowledge.snapshot.active-only --claim knowledge.snapshot.replay-stable`

Expected: 【推論】actuals green；unadmitted/live-requery negatives direct red。

- [ ] **Step 5: Commit**

```bash
git add 規格/知識/KnowledgeSnapshot.schema.json 規格/知識/保證 nova/權威/知識/檢索.py nova/權威/知識/快照.py nova/應用/建立知識快照.py 驗收/知識/測_快照重播.py
git commit -m "feat: freeze active knowledge into snapshots"
```

---

### Task 5: 傳遞來源撤銷並通知在途 Work

**Files:**
- Create: `nova/權威/知識/撤銷.py`
- Create: `nova/應用/撤銷知識.py`
- Modify: `nova/應用/test_知識服務.py`
- Create: `驗收/知識/測_撤銷傳遞.py`
- Create: `規格/知識/保證/撤銷傳遞.claim.json`

**Interfaces:**
- Produces: `invalidate_source(source_ref) -> tuple[AssertionReviewRequired, ...]` over transitive closure。
- Publishes: `KnowledgeRevokedSignal(snapshot_ref, affected_assertions)` to matching nonterminal Work。

**ClaimSpec:** 【推論】`knowledge.revocation.transitive` 從紅轉綠。

**固定負控:** 【推論】source S→A→B→C，撤銷 S 只改 A；B/C 若仍 clean ACTIVE 或新 snapshot 可選，direct red。

- [ ] **Step 1: 寫 deep/wide closure 與 alternate-source cases red**

```python
def test_source_revocation_marks_all_descendants() -> None:
    result = invalidate_source(chain("source", "a", "b", "c"))
    assert {r.assertion_id for r in result} == {"a", "b", "c"}
```

- [ ] **Step 2: 跑測試確認只有 direct child 被處理**

Run: `uv run pytest -q 驗收/知識/測_撤銷傳遞.py`

Expected: 【推論】FAIL。

- [ ] **Step 3: 寫 persisted BFS/closure 與 idempotent status events**

```python
affected = source_graph.descendants_of_source(source_ref)
return tuple(AssertionReviewRequired(ref, reason="SOURCE_INVALIDATED") for ref in sorted(affected))
```

- [ ] **Step 4: 跑 crash/restart、Work signal 與 ClaimSpec**

Run: `uv run pytest -q nova/應用/test_知識服務.py 驗收/知識/測_撤銷傳遞.py && uv run python 工具/跑驗收.py --claim knowledge.revocation.transitive`

Expected: 【推論】PASS；同 source 重送無 duplicate status，existing snapshot history unchanged。

- [ ] **Step 5: Commit**

```bash
git add nova/權威/知識/撤銷.py nova/應用/撤銷知識.py nova/應用/test_知識服務.py 驗收/知識/測_撤銷傳遞.py 規格/知識/保證/撤銷傳遞.claim.json
git commit -m "feat: propagate knowledge source revocation"
```

---

### Task 6: 讓索引可丟棄重建且不成第二份真相

**Files:**
- Create: `nova/基礎設施/知識索引/sqlite.py`
- Create: `nova/基礎設施/知識索引/重建.py`
- Create: `nova/基礎設施/知識索引/test_索引.py`
- Modify: `nova/權威/知識/檢索.py`

**Interfaces:**
- Produces: `rebuild_knowledge_index(events, content_store) -> IndexCheckpoint`。
- Produces: exact scope/status/adjacency projection at an authority event cursor；它只能加速 exact set/edge lookup。
- Consumes: projection rows, then revalidates status/scope/closure against authority fold before use；不得提供 ranked retrieval 或刪除任何 semantic candidate。

**ClaimSpec:** 【推論】`knowledge.index.rebuildable-nonauthoritative` 從紅轉綠。

**固定負控:** 【推論】索引保留已 revoked assertion、整檔刪除後重建，或 fake ANN 重建使 top-k 從 `A,B,C` 漂成 `A,B,D`；resolved snapshot 都必須與 authority fold 相同，不能採信 stale status 或 ranked result。

- [ ] **Step 1: 寫 stale-index/delete-rebuild red**

```python
def test_stale_index_cannot_reactivate_revoked_assertion() -> None:
    index = index_before_revocation()
    revoke("a@1")
    assert resolve_using_index(index, fixed_query()).assertion_refs == ()
```

- [ ] **Step 2: 跑測試確認 index status 被當真相**

Run: `uv run pytest -q nova/基礎設施/知識索引/test_索引.py`

Expected: 【推論】FAIL。

- [ ] **Step 3: 寫 event cursor rebuild 與 authority recheck**

```python
projection = index.project_exact_scope_status_adjacency(query.scope, at_cursor=query.event_cursor)
active_closure = authority.revalidate_and_close(projection, query.facts)
```

- [ ] **Step 4: 跑 parity property test 與 ClaimSpec**

Run: `uv run pytest -q nova/基礎設施/知識索引/test_索引.py && uv run python 工具/跑驗收.py --claim knowledge.index.rebuildable-nonauthoritative`

Expected: 【推論】PASS；任意 event prefix rebuild gives same semantic closure after authority recheck；checkpoint cursor、row-set digest或closure count不符就 full fold/rebuild，不能猜測補洞。

- [ ] **Step 5: Commit**

```bash
git add nova/基礎設施/知識索引 nova/權威/知識/檢索.py
git commit -m "feat: rebuild nonauthoritative knowledge index"
```

---

### Task 7: 機械阻止知識跨權威越寫

**Files:**
- Create: `驗收/知識/測_跨權威越界.py`
- Create: `規格/知識/保證/知識不得越權.claim.json`
- Modify: `nova/權威/知識/端口.py`

**Interfaces:**
- Allows: context/policy proposal and immutable snapshot refs only。
- Forbids: CriterionDefinition writer、Budget writer、EffectIntent writer imports/ports。

**ClaimSpec:** 【推論】`knowledge.authority.no-criterion-budget-effect-write` 從紅轉綠。

**固定負控:** 【推論】fixture 在 `nova/權威/知識/` import `nova.權威.資源.預算帳` 或 `nova.權威.效果.意圖`，或 fake port 暴露 activate/send；architecture test direct red。

- [ ] **Step 1: 寫 forbidden-import/interface red fixtures**

```python
def test_knowledge_ports_are_proposal_and_snapshot_only() -> None:
    assert set(public_methods(KnowledgeConsumerPort)) == {"resolve_snapshot", "load_snapshot", "propose_assertion"}
```

- [ ] **Step 2: 跑 architecture test 確認禁止規則尚未登錄**

Run: `uv run pytest -q 驗收/知識/測_跨權威越界.py`

Expected: 【推論】FAIL。

- [ ] **Step 3: 收窄 ports 並加入 import-linter rule**

```toml
[[tool.importlinter.contracts]]
name = "knowledge cannot write other authorities"
type = "forbidden"
source_modules = ["nova.權威.知識"]
forbidden_modules = ["nova.權威.判準", "nova.權威.資源", "nova.權威.效果"]
```

- [ ] **Step 4: 跑 ClaimSpec 與全知識 suite**

Run: `uv run pytest -q nova/權威/知識 nova/基礎設施/知識索引 nova/應用/test_知識服務.py 驗收/知識 -n 2 && uv run python 工具/跑驗收.py --prefix knowledge.`

Expected: 【推論】PASS；three forbidden imports direct red。

- [ ] **Step 5: Commit**

```bash
git add nova/權威/知識/端口.py 驗收/知識/測_跨權威越界.py 規格/知識/保證/知識不得越權.claim.json pyproject.toml
git commit -m "test: enforce knowledge authority boundaries"
```

---

### Task 8: 固定 deterministic query policy、context overflow 與 retrieval 反轉閘

**Files:**
- Create: `規格/知識/RetrievalSelectionPolicy.schema.json`
- Create: `規格/知識/保證/範圍圖全量決定性.claim.json`
- Create: `規格/知識/保證/索引重建語義同一.claim.json`
- Create: `規格/知識/保證/知識上下文不得靜默截斷.claim.json`
- Create: `規格/知識/保證/快取只准加速.claim.json`
- Create: `驗收/知識/測_決定性檢索政策.py`
- Create: `驗收/知識/測_retrieval反轉門檻.py`
- Modify: `規格/知識/初始知識政策.policy.json`
- Modify: `規格/知識/KnowledgeSnapshot.schema.json`
- Modify: `nova/權威/知識/檢索.py`
- Modify: `nova/權威/知識/快照.py`
- Modify: `nova/基礎設施/知識索引/sqlite.py`
- Modify: `nova/基礎設施/知識索引/重建.py`

**Interfaces:**
- Produces: `resolve_eligible_context(query_facts, policy_ref, event_cursor, now, backend_fingerprint, usable_context) -> KnowledgeSnapshotRef|KNOWLEDGE_CONTEXT_OVERFLOW`。
- Produces: `evaluate_retrieval_trigger(window) -> NoChange|RetrievalSelectionWork`；只開 ordinary Work，不直接啟用 retrieval。

**ClaimSpec:** 【推論】`knowledge.query.scope-dag-full-eligible`、`knowledge.snapshot.index-rebuild-semantic-parity`、`knowledge.context.no-silent-truncation`、`knowledge.cache.acceleration-only` 從紅轉綠。

**固定負控:** 【推論】①改用 SQL iteration order 令同層 DAG 節點換序；②fake ANN rebuild 把 kth item 從 `C` 換成 `D`；③超額時丟掉最後一條 eligible assertion 卻回 success；④cache miss 路徑摘要或重排 payload。四個 faulty subjects 必須各被指定 claim direct red。

- [ ] **Step 1: 寫六步 resolution、ANN rebuild、overflow 與 cache parity red**

```python
def test_index_rebuild_cannot_change_snapshot_membership() -> None:
    before = resolve_with(fake_ranked_index(("A", "B", "C")))
    after = resolve_with(fake_ranked_index(("A", "B", "D")))
    assert before.digest == after.digest == authority_full_context_digest()
```

- [ ] **Step 2: 跑四份驗收，確認 policy/schema/typed overflow 尚不存在**

Run: `uv run pytest -q 驗收/知識/測_決定性檢索政策.py 驗收/知識/測_retrieval反轉門檻.py`

Expected: 【推論】FAIL；至少缺 stable topological tie-break、`KNOWLEDGE_CONTEXT_OVERFLOW` 或 cache-byte parity。

- [ ] **Step 3: 寫六步 pure resolver、hard budget 與 acceleration-only cache key**

```python
limit = min(32_768, floor(usable_context_after_mandatory_reserves * 0.25))
refs = stable_topological_order(active_provenance_closure(exact_scope_matches))
if backend_token_count(canonical_payload(refs)) > limit:
    return KnowledgeContextOverflow(required_tokens=required, limit_tokens=limit, snapshot_digest=digest(refs))
```

- [ ] **Step 4: 寫 30-day measured trigger 與 RetrievalSelectionWork 五項 admission gate**

【推論】rolling window 必須同時滿 30 日且至少 1,000 次 dispatch；任一條才准提出 `RetrievalSelectionWork`：overflow rate `≥1%`、knowledge payload p95 `>25%` usable context、knowledge input cost p95 `>30%` execution input cost、或 full-fold p95 `>50 ms` 且 ACTIVE assertions `≥10,000`。proposal 不改現行 policy。

【推論】候選 retrieval 必須在至少 500 個事前固定 real query/gold sets 同時通過：required-assertion recall `≥0.99`、scope violation `=0`、下游 ClaimSpec pass rate 相對 full-context 下降 `≤1` percentage point、token 或 latency 至少改善 `30%`、100 次乾淨 index rebuild 的 snapshot digest parity `=100%`。任一不符就拒絕；不得以平均 embedding score 代替。

- [ ] **Step 5: 跑四份 ClaimSpec、索引重建與固定 retrieval gate negatives**

Run: `uv run pytest -q nova/基礎設施/知識索引 驗收/知識/測_決定性檢索政策.py 驗收/知識/測_retrieval反轉門檻.py -n 2 && uv run python 工具/跑驗收.py --claim knowledge.query.scope-dag-full-eligible --claim knowledge.snapshot.index-rebuild-semantic-parity --claim knowledge.context.no-silent-truncation --claim knowledge.cache.acceleration-only`

Expected: 【推論】PASS；四個固定 faulty subjects direct red；未滿 30 日／1,000 dispatch、五項 adoption gate 任一失敗都不能改 policy。

- [ ] **Step 6: Commit**

```bash
git add 規格/知識/RetrievalSelectionPolicy.schema.json 規格/知識/初始知識政策.policy.json 規格/知識/KnowledgeSnapshot.schema.json 規格/知識/保證/範圍圖全量決定性.claim.json 規格/知識/保證/索引重建語義同一.claim.json 規格/知識/保證/知識上下文不得靜默截斷.claim.json 規格/知識/保證/快取只准加速.claim.json nova/權威/知識/檢索.py nova/權威/知識/快照.py nova/基礎設施/知識索引/sqlite.py nova/基礎設施/知識索引/重建.py 驗收/知識/測_決定性檢索政策.py 驗收/知識/測_retrieval反轉門檻.py
git commit -m "feat: make knowledge resolution deterministic and full-context"
```

## Plan Exit Gate

- 【推論】untrusted caller 永遠只能 PROPOSED；active/revoke/supersede 只有 Knowledge Authority command。
- 【推論】provenance 是 typed graph且 cycle-free，不是 URL 欄；來源撤銷完整傳遞到所有 descendants。
- 【推論】snapshot 只含當下 ACTIVE revisions，之後 clock/index/restart 不改 snapshot digest。
- 【推論】index 可整個刪除重建，不掌握 status truth。
- 【推論】v1 resolution 是 exact scope→ACTIVE/TTL/profile→provenance closure→stable topo→full eligible context；沒有向量、GraphRAG entity extraction、summary tree或 ranked top-k。
- 【推論】context 放不下只回 `KNOWLEDGE_CONTEXT_OVERFLOW`；cache hit/miss canonical bytes相同。
- 【推論】retrieval 只能在 30-day/1,000-dispatch trigger 後以 `RetrievalSelectionWork` 提案，並通過 500-query五項 gate；不能自動降級。
- 【推論】Knowledge Authority 沒有直接寫 criterion/budget/effect 的 import 或 port。
- 【推論】`uv run pytest -q nova/權威/知識 nova/基礎設施/知識索引 nova/應用/test_知識服務.py 驗收/知識 -n 2` 與所有 `knowledge.*` ClaimSpecs 綠。

## Execution Handoff

【推論】本 plan 不實作 ConstraintSpec parsing 或上下文 packing；那是 plan 12 在既有 `ADVISORY_CONSTRAINT` profile 上增加的 consumer/extension，不是缺欄位。先完成本 plan，否則 plan 12 會把一個 prompt registry誤做成知識真相。
