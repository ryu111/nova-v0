# 判準評估與隔離回饋 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 【推論】讓判準定義與候選執行彼此隔離、讓裁定輸出有受控梯度，並確保任何一次揭露都不可逆地燒掉該 sealed case；宿主能力不足時 typed fail，絕不靜默降級。

**Architecture:** 【推論】判準面拆成 immutable definition authority、evaluation authority 與 feedback reducer。候選只取得 guidance pool 的公開條款；sealed pool 的檔案與 expected values 不進候選 workspace。評估器在 execution 結束後取得候選 CAS snapshot，以 `IsolationCapability` 協商可執行等級；reducer 只輸出 clause-level code、位置類別與 remediation class，不逐字轉送 assertion repr。

**Tech Stack:** 【推論】CPython 3.14.7、ClaimSpec/TestPlan、MachineSpec、temporary filesystem projection、subprocess evaluator、CAS evidence、SQLite state owner。

**Spec:** 【查證】本檔「子系統規格」，以及[第一輪性質 4 解法](../sol-新局-第一輪.md)、[第二輪威脅模型與 ClaimSpec](../sol-新局-第二輪.md)、[第三輪 isolation 裁決](../sol-新局-第三輪.md)、[第四輪約束與隔離更正](../sol-新局-第四輪.md)。

## Global Constraints

- 【推論】v1 明講只防「無意看到」；同 UID、無 sandbox 時不宣稱抵抗主動 syscall／`/proc`／socket 竊取。
- 【推論】每個 ClaimSpec 必填 `isolation.required_capabilities`；offer 不覆蓋 requirement 時結果只能是 `UNSUPPORTED_ISOLATION`。
- 【推論】guidance 與 sealed case 使用不同 content roots、不同 projection manifest、不同 process invocation；候選 invocation 不帶 sealed path、digest 或 expected value。
- 【推論】回饋粒度固定為 `clause_id + outcome_class + location_bucket + retryability`；原始 stdout/stderr 只進 evidence，不能直接回給執行者。
- 【推論】sealed case 一旦 raw detail 被授權揭露，當次 evaluation 可完成，但 case 立即 `BURNED`，不得再用於後續 admission。

## 子系統規格

【推論】`CriterionDefinition` 指向 guidance cases 與 sealed cases 的 immutable refs；`EvaluationRun` 狀態為 `REQUESTED -> ISOLATION_CHECKED -> RUNNING -> REDUCED -> VERDICT_RECORDED`，另有 typed terminals `UNSUPPORTED_ISOLATION`、`HARNESS_FAULT`、`CANCELLED`。

【推論】`Verdict` 是 evaluation authority 的產物，包含每條 claim 的 typed result與 evidence refs；它不包含 ClaimSpec source bytes。`FeedbackPacket` 是衍生物，不是 verdict 本體。

【推論】v1 capability vocabulary 固定為 `SEPARATE_PROCESS`、`OMIT_SEALED_PATHS`、`ENV_ALLOWLIST`、`NO_INHERITED_FDS`、`NETWORK_POLICY_DECLARED`；其中前四項可以在目前部署提供，`NETWORK_DENY_ENFORCED`、`PROC_HIDE`、`UID_SEPARATION`、`MICROVM` 只可被宣告為 unsupported。

## File Structure

```text
規格/判準/
├── 評估.machine.json                         — evaluation lifecycle 與 typed terminals。
├── 揭露帳.machine.json                       — ReserveDisclosure／Recorded／Exhausted。
├── CriterionDefinition.schema.json           — guidance/sealed refs 與 admission metadata。
├── DisclosureLedger.schema.json              — lineage、disclosure_id、digest、ordinal。
├── FeedbackPolicy.schema.json                 — clause-level reducer policy。
├── IsolationCapability.schema.json            — requirement/offer capability vocabulary。
├── RevealApproval.schema.json                 — 揭露核准封閉形狀；核准者由 attestation 承載。
└── 保證/
    ├── sealed內容不進候選.claim.json          — projection/env/argv 都不含 sealed refs。
    ├── 隔離不得靜默降級.claim.json            — unsupported capability typed terminal。
    ├── 回饋經reducer.claim.json               — assertion repr 不逐字外洩。
    ├── 揭露即燒掉.claim.json                  — revealed case cannot be reused。
    ├── 揭露須經核准.claim.json                — 六格核准負控；無 binding 即 fail-closed。
    └── 揭露總量有界.claim.json                — cap 跨 run、跨 revision、跨 sibling 累計。
nova/權威/判準/
├── 定義.py                                   — immutable definition admission/read API。
├── 評估.py                                   — EvaluationAggregate 與 verdict creation。
├── 隔離協商.py                               — required subset offered 純判定。
├── 回饋閘.py                                 — raw result -> FeedbackPacket reducer。
├── 案例治理.py                               — ACTIVE/BURNED/REVOKED lifecycle。
├── 揭露核准.py                               — RevealApprovalVerifier port＋fail-closed 預設。
├── 揭露帳本.py                               — 純 fold：事件序列 → 剩餘額度。
└── test_判準權威.py                          — domain and admission tests。
nova/基礎設施/裁定執行/
├── 投影.py                                   — candidate/evaluator filesystem manifests。
├── 隔離執行.py                               — process/env/fd setup and offer evidence。
├── evaluator_entry.py                        — fresh-process TestPlan entry point。
└── test_隔離執行.py                          — argv/env/path leak tests。
nova/應用/
├── 執行判準.py                               — definition -> isolation -> evaluator -> verdict。
└── test_執行判準.py                          — orchestration and fault taxonomy。
驗收/判準/
├── fixtures/                                 — public guidance 與 private sealed fixture builders。
├── 測_雙測試池.py                            — candidate projection cannot enumerate sealed pool。
├── 測_隔離協商.py                            — requirement subset/no downgrade。
├── 測_回饋縮減.py                            — expected repr canary absent from packet。
├── 測_揭露燒毀.py                            — reveal then reuse direct rejection。
└── 測_揭露帳本.py                            — crash matrix、lineage 額度、cap 拒絕。
```

## Dependency Gate

前置計畫：01 02 03 04 05

【推論】必須先完成 plan 01–05。沒有 ClaimSpec control semantics，sealed negative 可能是假紅；沒有 MachineSpec/state owner，case burn 與 verdict 會在 crash 後分叉；沒有 CAS，原始 evidence 會被塞入 feedback；沒有 execution envelope，evaluator 自身沒有外部上限。缺任何一項都不能宣稱判準權在執行者之外。

---

### Task 1: 宣告 CriterionDefinition 與 Evaluation lifecycle

**Files:**
- Create: `規格/判準/評估.machine.json`
- Create: `規格/判準/CriterionDefinition.schema.json`
- Create: `nova/權威/判準/定義.py`
- Create: `nova/權威/判準/評估.py`
- Create: `nova/權威/判準/test_判準權威.py`

**Interfaces:**
- Produces: `CriterionDefinitionRef`、`CaseRef(pool, digest, lifecycle)`。
- Produces: `decide_evaluation(state, command) -> DomainEvent[]`。

**ClaimSpec:** 【推論】`criterion.lifecycle.definition-verdict-separated` 從紅轉綠。

**固定負控:** 【推論】在 mutable `EvaluationRun` object 內直接改 ClaimSpec expected value；model/schema 必須沒有該欄位並拒絕 unknown mutation。

- [ ] **Step 1: 寫 definition immutable／verdict separate 的 red tests**

```python
def test_評估執行不持有可寫判準內容() -> None:
    run = EvaluationRun.requested(definition_ref=fixed_definition_ref())
    assert not hasattr(run, "expected")
    with pytest.raises(FrozenInstanceError):
        run.definition_ref = other_ref()
```

- [ ] **Step 2: 跑測試確認 types/schema 缺失**

Run: `uv run pytest -q nova/權威/判準/test_判準權威.py -k separated`

Expected: 【推論】FAIL with missing types。

- [ ] **Step 3: 寫封閉 schema、immutable refs 與 machine decision**

```python
@dataclass(frozen=True, slots=True)
class CriterionDefinition:
    definition_id: SemanticId
    revision: int
    guidance_cases: tuple[CaseRef, ...]
    sealed_cases: tuple[CaseRef, ...]
    claims: tuple[ClaimRef, ...]
```

- [ ] **Step 4: 跑 schema/machine/unit tests**

Run: `uv run pytest -q nova/權威/判準/test_判準權威.py 驗收/狀態機/測_所有宣告.py`

Expected: 【推論】PASS；每個 nonterminal 可達 typed terminal。

- [ ] **Step 5: 跑 named mutable-definition negative**

Run: `uv run python 工具/跑驗收.py --claim criterion.lifecycle.definition-verdict-separated`

Expected: 【推論】actual `ACCEPT`、negative direct `CLAIM_REJECTED`。

- [ ] **Step 6: Commit**

```bash
git add 規格/判準/評估.machine.json 規格/判準/CriterionDefinition.schema.json nova/權威/判準
git commit -m "feat: 判準定義與裁定執行分離"
```

---

### Task 2: 建立 isolation capability 協商且拒絕降級

**Files:**
- Create: `規格/判準/IsolationCapability.schema.json`
- Create: `nova/權威/判準/隔離協商.py`
- Modify: `nova/權威/判準/評估.py`
- Create: `驗收/判準/測_隔離協商.py`
- Create: `規格/判準/保證/隔離不得靜默降級.claim.json`

**Interfaces:**
- Produces: `negotiate(required: frozenset[Capability], offered: IsolationOffer) -> IsolationGrant | UnsupportedIsolation`。
- Produces: offer evidence with host mechanism/version。

**ClaimSpec:** 【推論】`criterion.isolation.no-silent-downgrade` 從紅轉綠。

**固定負控:** 【推論】ClaimSpec 要求 `NETWORK_DENY_ENFORCED`，宿主只宣告 `NETWORK_POLICY_DECLARED`；不得開始 evaluator，結果恰為 `UNSUPPORTED_ISOLATION`。

- [ ] **Step 1: 寫 subset table 與近似名稱負控**

```python
def test_較弱能力不能冒充較強能力() -> None:
    result = negotiate({Capability.NETWORK_DENY_ENFORCED}, offer(Capability.NETWORK_POLICY_DECLARED))
    assert result == UnsupportedIsolation(missing={Capability.NETWORK_DENY_ENFORCED})
```

- [ ] **Step 2: 跑測試確認近似能力錯誤通過或 module 缺失**

Run: `uv run pytest -q 驗收/判準/測_隔離協商.py`

Expected: 【推論】FAIL。

- [ ] **Step 3: 寫 exact set inclusion 與封閉 vocabulary**

```python
missing = required.difference(offered.capabilities)
return UnsupportedIsolation(frozenset(missing)) if missing else IsolationGrant(offered.evidence_ref)
```

- [ ] **Step 4: 跑 property tests 與 ClaimSpec**

Run: `uv run pytest -q 驗收/判準/測_隔離協商.py && uv run python 工具/跑驗收.py --claim criterion.isolation.no-silent-downgrade`

Expected: 【推論】PASS；任意 required set 缺一 capability 都不啟動 evaluator。

- [ ] **Step 5: Commit**

```bash
git add 規格/判準/IsolationCapability.schema.json 規格/判準/保證/隔離不得靜默降級.claim.json nova/權威/判準 驗收/判準/測_隔離協商.py
git commit -m "feat: 拒絕不支援的判準隔離"
```

---

### Task 3: 產生互斥 candidate/evaluator projection

**Files:**
- Create: `nova/基礎設施/裁定執行/投影.py`
- Create: `nova/基礎設施/裁定執行/隔離執行.py`
- Create: `nova/基礎設施/裁定執行/evaluator_entry.py`
- Create: `nova/基礎設施/裁定執行/test_隔離執行.py`
- Create: `驗收/判準/fixtures/__init__.py`
- Create: `驗收/判準/測_雙測試池.py`
- Create: `規格/判準/保證/sealed內容不進候選.claim.json`

**Interfaces:**
- Produces: `ProjectionManifest(candidate_paths, evaluator_paths, env_keys, inherited_fds)`。
- Produces: `run_evaluator(test_plan_ref, candidate_snapshot_ref, grant) -> RawEvaluationEvidence`。

**ClaimSpec:** 【推論】`criterion.sealed-content.absent-from-candidate-projection` 從紅轉綠。

**固定負控:** 【推論】sealed fixture 放入 unique canary `EXPECTED-9f72c1a4e0b6`；候選 process 遞迴讀 workspace、argv、env 與繼承 fd，任何 bytes 出現 canary 都使 claim direct red。

- [ ] **Step 1: 寫 canary enumeration red**

```python
def 測試_候選投影找不到_sealed_canary() -> None:
    observed = run_candidate_enumerator(build_two_pool_fixture())
    assert SEALED_CANARY.encode() not in observed.all_visible_bytes
```

- [ ] **Step 2: 跑測試確認共用 fixture root 洩漏**

Run: `uv run pytest -q 驗收/判準/測_雙測試池.py`

Expected: 【推論】FAIL until projections are separated。

- [ ] **Step 3: 寫兩份 allowlist manifest 與 fresh evaluator process**

```python
candidate_manifest = ProjectionManifest(paths=guidance_paths | candidate_input_paths, env_keys=SAFE_ENV, inherited_fds=frozenset())
evaluator_manifest = ProjectionManifest(paths=sealed_paths | {candidate_snapshot_path, test_plan_path}, env_keys=SAFE_ENV, inherited_fds=frozenset())
```

- [ ] **Step 4: 跑 visibility tests、process evidence 與 ClaimSpec**

Run: `uv run pytest -q nova/基礎設施/裁定執行/test_隔離執行.py 驗收/判準/測_雙測試池.py && uv run python 工具/跑驗收.py --claim criterion.sealed-content.absent-from-candidate-projection`

Expected: 【推論】PASS；candidate manifest digest 不含 sealed refs，evaluator 可讀 sealed cases。

- [ ] **Step 5: Commit**

```bash
git add nova/基礎設施/裁定執行 驗收/判準 規格/判準/保證/sealed內容不進候選.claim.json
git commit -m "feat: 隔離封存判準的投影"
```

---

### Task 4: 產生 typed verdict 並區分 harness fault

**Files:**
- Create: `nova/應用/執行判準.py`
- Create: `nova/應用/test_執行判準.py`
- Modify: `nova/權威/判準/評估.py`
- Modify: `nova/基礎設施/裁定執行/evaluator_entry.py`

**Interfaces:**
- Produces: `EvaluationService.evaluate(request) -> Verdict | EvaluationTerminal`。
- Persists: raw evidence CAS ref、TestPlan digest、candidate snapshot digest、per-claim CaseResult。

**ClaimSpec:** 【推論】`criterion.verdict.harness-fault-not-candidate-failure` 從紅轉綠。

**固定負控:** 【推論】讓 predicate plugin import failure；不得產生 `CLAIM_REJECTED` 或讓 pursuit 將它當候選失敗，必須 `HARNESS_FAULT`。

- [ ] **Step 1: 寫三分 taxonomy red（accept/reject/harness）**

```python
@pytest.mark.parametrize(("fixture", "kind"), [("pass", "ACCEPT"), ("fixed-fault", "CLAIM_REJECTED"), ("broken-runner", "HARNESS_FAULT")])
async def test_verdict_taxonomy(fixture: str, kind: str) -> None:
    result = await evaluate_fixture(fixture)
    assert result.kind == kind
```

- [ ] **Step 2: 跑測試確認 runner exception 冒充 reject**

Run: `uv run pytest -q nova/應用/test_執行判準.py`

Expected: 【推論】FAIL at broken-runner case。

- [ ] **Step 3: 寫明確 exception boundary 與 evidence-first persist**

```python
try:
    raw = evaluator.run(test_plan, candidate)
except EvaluatorInfrastructureError as error:
    return EvaluationTerminal.harness_fault(evidence_store.put(error.as_evidence()))
return verdict_authority.record(reduce_case_results(raw))
```

- [ ] **Step 4: 跑 taxonomy 與 crash-before-verdict tests**

Run: `uv run pytest -q nova/應用/test_執行判準.py -n 2`

Expected: 【推論】PASS；相同 evaluation id 重送只產生一個 verdict。

- [ ] **Step 5: Commit**

```bash
git add nova/權威/判準/評估.py nova/基礎設施/裁定執行/evaluator_entry.py nova/應用/執行判準.py nova/應用/test_執行判準.py
git commit -m "feat: 記錄帶型別的判準裁定"
```

---

### Task 5: 只輸出 clause-level gated feedback

**Files:**
- Create: `規格/判準/FeedbackPolicy.schema.json`
- Create: `nova/權威/判準/回饋閘.py`
- Modify: `nova/應用/執行判準.py`
- Create: `驗收/判準/測_回饋縮減.py`
- Create: `規格/判準/保證/回饋經reducer.claim.json`

**Interfaces:**
- Produces: `reduce_feedback(verdict, policy) -> FeedbackPacket`。
- Produces: fixed enums `OutcomeClass`、`LocationBucket`、`Retryability`。

**ClaimSpec:** 【推論】`criterion.feedback.clause-level-reduced` 從紅轉綠。

**固定負控:** 【推論】raw assertion 為 `expected SECRET-ANSWER-441, got 0`；FeedbackPacket canonical bytes 不得含 `SECRET`、`ANSWER`、`441` 或 raw repr，但要含失敗 `clause_id`。

- [ ] **Step 1: 寫 secret canary absence 與 gradient presence red**

```python
def test_feedback_has_clause_without_expected_repr() -> None:
    packet = reduce_feedback(verdict_with_raw("expected SECRET-ANSWER-441, got 0"), fixed_policy())
    encoded = canonical_json_bytes(packet)
    assert b"clause.limit.enforced" in encoded
    assert b"SECRET-ANSWER-441" not in encoded
```

- [ ] **Step 2: 跑測試確認 raw stderr 被轉送**

Run: `uv run pytest -q 驗收/判準/測_回饋縮減.py`

Expected: 【推論】FAIL at canary absence。

- [ ] **Step 3: 寫 allowlist reducer；未知 raw class 歸 `UNCLASSIFIED`**

```python
return FeedbackPacket(items=tuple(FeedbackItem(clause_id=r.clause_id, outcome_class=map_outcome(r), location_bucket=map_location(r), retryability=map_retry(r)) for r in verdict.results))
```

- [ ] **Step 4: 跑 reducer property tests 與 ClaimSpec**

Run: `uv run pytest -q 驗收/判準/測_回饋縮減.py && uv run python 工具/跑驗收.py --claim criterion.feedback.clause-level-reduced`

Expected: 【推論】PASS；任意 raw UTF-8 string 不會直接出現在 packet。

- [ ] **Step 5: Commit**

```bash
git add 規格/判準/FeedbackPolicy.schema.json 規格/判準/保證/回饋經reducer.claim.json nova/權威/判準/回饋閘.py nova/應用/執行判準.py 驗收/判準/測_回饋縮減.py
git commit -m "feat: 判準回饋逐條把關"
```

---

### Task 6: 實作「揭露即燒掉」且 crash-safe

**Files:**
- Create: `nova/權威/判準/案例治理.py`
- Create: `nova/權威/判準/揭露核准.py`
- Create: `規格/判準/RevealApproval.schema.json`
- Create: `規格/判準/保證/揭露須經核准.claim.json`
- Modify: `nova/權威/判準/定義.py`
- Modify: `nova/應用/執行判準.py`
- Create: `驗收/判準/測_揭露燒毀.py`
- Create: `規格/判準/保證/揭露即燒掉.claim.json`

**Interfaces:**
- Produces: `RevealApproval`（封閉形狀）——`criterion_revision`／`criterion_digest`、
  exact `case_ref`、`requested_action = REVEAL_RAW_CASE`、`approver_attestation_ref`、
  `authority_revision`／`authority_digest`、`issued_at`、`expiry` 或明確 one-shot、`nonce`。
- Produces: `RevealApprovalVerifier` port——
  `verify(approval, criterion_revision, case_ref, requested_action)
  -> VerifiedRevealApproval | ApprovalFailure`。`authorize_reveal` **只收**
  `VerifiedRevealApproval`；**無 production binding 時一律
  `APPROVAL_AUTHORITY_UNAVAILABLE`**——raw reveal 是安全邊界，fail-closed 不降名。
  production binding 的落點＝計畫 12 權威閘（明文，本 task 只交付 port、schema、
  fake verifier 與 fail-closed 預設）。
  （R10→R11 修正：只加 schema 不夠——沒有任何已存在的 authority 能判斷
  `approver_attestation_ref` 是否有效，「有 schema 不等於能驗」。）
- Produces: `authorize_reveal(case_ref, verified_approval) -> RevealReceipt`。
- Produces: atomic `CaseBurned` event before raw reveal bytes are returned。

**ClaimSpec:** 【推論】`criterion.sealed-case.reveal-burns-before-release` 與 `criterion.sealed-case.reveal-requires-verified-approval` 從紅轉綠。

【推論】**五個寫入條件**（審查條件，2026-08-28）：
①production `VerifiedRevealApproval` **只能由 `RevealApprovalVerifier` 的成功結果取得**
——否則只是把 `approval_envelope` 改個名字。
②`authorize_reveal` 必須驗證 verification evidence 與 approval digest、criterion revision/digest、
exact case ref、requested action、authority revision/digest 全部綁定。
③新增固定負控 `forged-verified-wrapper` → `verified_approval_requires_verifier_evidence`：
**直接手造 `VerifiedRevealApproval` 不得取得 reveal**。
④`validity` 必須是 discriminated union `EXPIRES_AT(timestamp) | ONE_SHOT(nonce)`，
**不得是數個可同時缺席的 optional 欄位**。
⑤one-shot 的消費必須與 `CaseBurned` 在**同一個 state-owner transaction** 內落盤，
不得只靠程序記憶體 set。plan 12 production binding 未完成前，raw reveal 一律
`APPROVAL_AUTHORITY_UNAVAILABLE`；**fake verifier 只證明 contract，
不得讓 release gate 宣稱 production approval 已可用**。

**固定負控:** 【推論】在 `CaseBurned` commit 前／後 SIGKILL；commit 前不得收到 raw detail，commit 後重啟不得再次選中該 case。
核准六格：`unresolvable-attestation`（attestation ref 指向不可驗來源）、
`binds-other-case`（approval 綁另一個 case_ref）、`binds-other-criterion-revision`、
`approves-other-action`（`requested_action` 不是 `REVEAL_RAW_CASE`）、
`approval-replayed`（同 nonce／one-shot 重播第二次）——五者必須紅在
`reveal_requires_verified_approval`；`verifier-unavailable`（無 production binding
時送任何 reveal）必須紅在 `approval_authority_unavailable_fails_closed`。
防恆真格：合法 approval 經 fake verifier 放行，burn-before-release 流程照舊。

- [ ] **Step 1: 寫 ordering 與兩 crash points red**

```python
def test_reveal_is_after_burn_commit() -> None:
    trace = reveal_with_crash("after_burn_before_return")
    assert trace.committed("CaseBurned")
    assert not select_for_evaluation(trace.case_ref).is_selectable
```

- [ ] **Step 2: 跑測試確認 reveal/burn 非原子順序**

Run: `uv run pytest -q 驗收/判準/測_揭露燒毀.py`

Expected: 【推論】FAIL at one crash point。

- [ ] **Step 3: 先 append-once `CaseBurned`，再從 CAS 釋出 raw bytes**

```python
receipt = state_owner.transition(case_ref, command=BurnForReveal(approval_ref), idempotency_key=reveal_key)
return RevealPayload(receipt=receipt, bytes=evidence_store.get(case_ref.content_ref))
```

- [ ] **Step 4: 跑 crash matrix 與 ClaimSpec**

Run: `uv run pytest -q 驗收/判準/測_揭露燒毀.py && uv run python 工具/跑驗收.py --claim criterion.sealed-case.reveal-burns-before-release`

Expected: 【推論】PASS；named reuse negative direct red。

- [ ] **Step 5: Commit**

```bash
git add nova/權威/判準 規格/判準/RevealApproval.schema.json 規格/判準/保證/揭露須經核准.claim.json nova/應用/執行判準.py 驗收/判準/測_揭露燒毀.py 規格/判準/保證/揭露即燒掉.claim.json
git commit -m "feat: 封存 case 揭露前先燒掉"
```

---

### Task 7: 驗證目前威脅聲明沒有越界

**Files:**
- Modify: `規格/判準/IsolationCapability.schema.json`
- Modify: `nova/基礎設施/裁定執行/隔離執行.py`
- Create: `驗收/判準/測_威脅聲明.py`

**Interfaces:**
- Produces: `IsolationOffer.claimed_capabilities` only from mechanism probes。
- Produces: deployment report that lists unsupported hostile-isolation capabilities explicitly。

**ClaimSpec:** 【推論】`criterion.isolation.claims-match-probed-mechanisms` 從紅轉綠。

**固定負控:** 【推論】把同 UID 的 separate-process offer 加上 `PROC_HIDE` 或 `NETWORK_DENY_ENFORCED`，但沒有對應 probe evidence；offer admission 必須 red。

- [ ] **Step 1: 寫 capability-to-evidence exhaustive table red**

```python
def test_every_claimed_capability_has_matching_probe() -> None:
    offer = current_host_offer()
    assert set(offer.capabilities) == {probe.capability for probe in offer.probes if probe.passed}
```

- [ ] **Step 2: 跑 fabricated-offer negative**

Run: `uv run pytest -q 驗收/判準/測_威脅聲明.py`

Expected: 【推論】FAIL until admission validates probe refs。

- [ ] **Step 3: 寫 probe registry 與不可宣稱 capability 清單**

```python
HOSTILE_CAPABILITIES = frozenset({Capability.PROC_HIDE, Capability.UID_SEPARATION, Capability.MICROVM, Capability.NETWORK_DENY_ENFORCED})
```

- [ ] **Step 4: 跑 ClaimSpec；確認 v1 report 明列弱隔離**

Run: `uv run pytest -q 驗收/判準/測_威脅聲明.py && uv run python 工具/跑驗收.py --claim criterion.isolation.claims-match-probed-mechanisms`

Expected: 【推論】PASS；同 UID offer 不含四個 hostile capabilities。

- [ ] **Step 5: Commit**

```bash
git add 規格/判準/IsolationCapability.schema.json nova/基礎設施/裁定執行/隔離執行.py 驗收/判準/測_威脅聲明.py
git commit -m "test: 把隔離 claim 綁到宿主探針"
```

---

### Task 8: 揭露帳是 state owner 管的持久 aggregate

**Files:**
- Create: `規格/判準/揭露帳.machine.json`
- Create: `規格/判準/DisclosureLedger.schema.json`
- Create: `nova/權威/判準/揭露帳本.py`
- Create: `驗收/判準/測_揭露帳本.py`
- Create: `規格/判準/保證/揭露總量有界.claim.json`
- Modify: `規格/判準/CriterionDefinition.schema.json`
- Modify: `規格/判準/FeedbackPolicy.schema.json`
- Modify: `nova/應用/執行判準.py`

**Interfaces:**
- Produces: `揭露帳.machine.json`——明示 machine，command `ReserveDisclosure`、
  event `DisclosureRecorded`／`DisclosureExhausted`。應用層只能呼叫
  `StateOwnerClient.execute(CommandEnvelope)`（計畫 03），
  `CommandEnvelope.entity_id = sealed_pool_lineage_id`，**不得直接 append event**。
- Produces: 順序契約——transition transaction 之前，先把 canonical FeedbackPacket bytes
  放入 CAS（計畫 04）；event 必記 `disclosure_id`、`packet_content_ref`、`packet_digest`、
  `sealed_pool_lineage_id`、`ordinal`；`DisclosureRecorded` commit 成功後才釋出 packet bytes
  ——與本計畫 Task 6「先 `CaseBurned` 再 reveal」同一個已驗證形狀。
- Produces: crash 後從 CAS 以 `packet_content_ref` 取回**完全相同的 bytes** 重送同一
  `disclosure_id`，不重跑 reducer、不計新額度——「重新執行大概會一樣」不是保證。
- Produces: machine 必須拒絕——同 `disclosure_id` 不同 `packet_digest`；`ordinal` 超
  `disclosure_cap`；`sealed_pool_lineage_id` 不符；三者為 machine spec 內的固定負控，
  由 `工具/驗規格.py` 驗（計畫 02 的既有形狀）。
- Produces: `sealed_pool_lineage_id`——sealed pool 首建時鑄的內容定址 lineage id；
  criterion revision supersede 與 sibling／superseding Pursuit 都承繼，額度跨它們累計。
  `CriterionDefinition` 增必填 `sealed_pool_lineage_id`；`FeedbackPolicy` 增必填
  `disclosure_cap`（事前釘）。
- Forbids: `nova/權威/判準/揭露帳本.py` 做 I/O——它只做純 fold／決策
  （事件序列 → 剩餘額度／`DISCLOSURE_BUDGET_EXHAUSTED`），`allow_io=false` 不衝突；
  持久化與 I/O 全走應用層的 state owner port。

**為什麼**：Task 5／6 的保證綠著，sealed 判準的辨識力卻跨 run 靜默流失——
揭露帳若只活在權威層記憶體，程序重啟即洗掉 cap，而負控只測同一程序內的第 cap+1 次。
持久性交給已存在的 state owner（06 前置含 03），key 換成洗不掉的 lineage。
地基：Dwork 等 arXiv:1506.02629 Theorem 17——只取「有限 transcript range ⇒ 有限
max-information 上界」這個形狀，|Y| 按整段 transcript 計、adaptive composition 下累加，
所以 key 必須是跨 revision／Pursuit 的 lineage 而不是單次 run（正式引用時區分
Theorem 17 的有限 range bound 與 adaptive composition lemma）；NIST FRVT／SRE 的
提交限流是實務存在、定性、不對題——照實標。cap 數值與「先記帳再釋出」的具體形狀：
無地基，這是 nova 的拆解決定（Task 6 既有模式的重用）。宣稱 DP／統計有效性的請求
→ `UNSUPPORTED_DISCLOSURE_MECHANISM`。
加蓋：超 cap 停發（verdict 照記，只斷回饋）；lineage 額度不可被 revision／sibling 洗掉；
commit 前不得釋出；重送只認同 digest。

**ClaimSpec:** 【推論】`criterion.disclosure.transcript-bounded` 從紅轉綠。

**ClaimSpec落點:** `criterion.disclosure.transcript-bounded` → `規格/判準/保證/揭露總量有界.claim.json`（本 task Create）

【推論】**五個寫入條件，缺一額度就洗得掉**（審查條件，2026-08-28）：
①`ordinal` 由 aggregate 依**已提交**的唯一 disclosure 數量產生，**呼叫端不得提交或指定**。
②`ReserveDisclosure` 的冪等鍵是 lineage 內唯一的 `disclosure_id`：同 id 同 digest 回傳既有
reservation **不重扣**；同 id 不同 digest typed reject；新 id 而額度已滿則拒絕**且不得 release**。
③`sealed_pool_lineage_id` 必須是**首建時鑄造、後代繼承**的穩定身分，
**不得是會隨 revision 內容變化的當前集合 digest**——會變的 key 等於沒有 key。
④reserve 失敗或 commit 前 crash 留下的 CAS blob 是**不可達 orphan**：必須明講可由 GC 回收，
且候選**不能僅憑猜測 content ref 取得 sealed bytes**。
⑤`DisclosureExhausted` 不得在每次超額重試時無限追加；同一 command/id 必須冪等，
或讓 exhaustion 成為 aggregate 的既有狀態投影。

**固定負控:** 【推論】五格。`crash-then-reset`：cap 前 SIGKILL、重啟後從零計數繼續釋出
的變體，必須紅在 `ledger_survives_restart`。`sibling-resets-budget`：superseding Pursuit
拿新額度的變體，與 `revision-resets-budget`（換 criterion revision、同 lineage 拿新額度）
皆必須紅在 `lineage_scoped_budget`。`release-before-commit`：ledger commit 前就 return
packet bytes 的變體，必須紅在 `disclose_after_persist`。`disclosure-beyond-cap`：
cap+1 次照發的變體，必須紅在 `disclosure_beyond_budget_refused`。
machine 層三格固定負控住在 `揭露帳.machine.json` 內（同 id 不同 digest／ordinal 超 cap／
lineage 不符），由 `工具/驗規格.py --含固定負控` 驗。
防恆真格：cap 內正常 feedback 逐次照發；crash 後重送同 `disclosure_id` 同 digest
不重複扣額度；cap 滿後 verdict 記錄照常。

- [ ] **Step 1: 寫五個負控與防恆真格的 red tests（SIGKILL matrix 沿用 Task 6 形狀）**

```python
def test_崩潰重啟後額度不得歸零() -> None:
    軌跡 = 揭露到(3, 政策(disclosure_cap=5), 崩潰於="第三次commit後")
    重啟 = 重建帳本(軌跡.資料根)
    assert 重啟.已揭露 == 3
```

- [ ] **Step 2: 跑紅測確認今天重啟就洗掉 cap**

Run: `uv run pytest -q 驗收/判準/測_揭露帳本.py`

Expected: 【推論】FAIL；揭露計數只活在程序記憶體，重啟後從零開始。
不得是 `ModuleNotFoundError` 冒充紅測。

- [ ] **Step 3: 寫 machine spec 並驗固定負控**

Run: `uv run python 工具/驗規格.py 規格/判準/揭露帳.machine.json --含固定負控`

Expected: 【推論】PASS；同 id 不同 digest、ordinal 超 cap、lineage 不符三格
machine 負控各自 direct red。

- [ ] **Step 4: 寫純 fold 帳本與應用層順序（CAS → ReserveDisclosure → commit → release）**

- [ ] **Step 5: 跑五個負控、防恆真格與 ClaimSpec**

Run: `uv run pytest -q 驗收/判準/測_揭露帳本.py && uv run python 工具/跑驗收.py --claim criterion.disclosure.transcript-bounded`

Expected: 【推論】PASS；五個負控各紅在自己宣告的 predicate。

- [ ] **Step 6: Commit**

```bash
git add 規格/判準/揭露帳.machine.json 規格/判準/DisclosureLedger.schema.json 規格/判準/保證/揭露總量有界.claim.json 規格/判準/CriterionDefinition.schema.json 規格/判準/FeedbackPolicy.schema.json nova/權威/判準/揭露帳本.py nova/應用/執行判準.py 驗收/判準/測_揭露帳本.py
git commit -m "feat: 揭露帳持久化，額度跨 run 不歸零"
```

---

## Plan Exit Gate

- 【推論】候選 projection 的可見 bytes、argv、env、fds 都不含 sealed canary；evaluator projection 仍能執行 sealed cases。
- 【推論】required capability 缺一項即 `UNSUPPORTED_ISOLATION`，沒有 fallback branch。
- 【推論】raw assertion repr 不進 FeedbackPacket；clause id 與 retry class 保留最小梯度。
- 【推論】揭露先 burn 後 release，全部 crash points 後 case 都不會被重用。
- 【推論】`uv run pytest -q nova/權威/判準 nova/基礎設施/裁定執行 nova/應用/test_執行判準.py 驗收/判準 -n 2` 與本 plan ClaimSpecs 全綠。

## Execution Handoff

【推論】Task 3 與 Task 7 的審查者要逐項比對「宣稱能力」與真實機制，不接受以文件宣稱取代 probe。完成本 plan 與 plan 07 後，才有資格實作 Pursuit feedback loop。
