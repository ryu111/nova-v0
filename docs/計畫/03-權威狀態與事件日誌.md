# 權威狀態與事件日誌 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 【推論】建立唯一 state owner、declared-transition journal與可獨立尾隨 replica；任一 transaction boundary SIGKILL 後只靠持久 bytes 恢復，不遺失 committed event、不接受非法 edge、不產生兩個有效 lease。

**Architecture:** 【推論】`權威狀態.sqlite3` 用 one process／one connection／rollback／FULL，所有 worker 只送 typed command；同一 transaction append event、更新 head、寫 owner ledgers。commit 後 single publisher 按 global_seq 冪等複製到 `尾隨事件.sqlite3` WAL；tail 可砍，權威 journal 不可由 reader 寫。

**Tech Stack:** 【推論】CPython 3.14.7、stdlib sqlite3、multiprocessing／Unix socket、SQLite foreign keys、rollback journal、WAL tail、pytest subprocess SIGKILL、ClaimSpec runner。

**Spec:** 【查證】本檔「子系統規格」、[第三輪 runtime 三道硬閘與事件](../sol-新局-第三輪.md#27-執行時禁止未宣告轉移三道硬閘)、[第二輪 workload envelope](../sol-新局-第二輪.md#32-建議的-v1-design-envelope)。第五輪 archive／prune 由 Plan 04 承接。

## Global Constraints

- 【推論】Plan 01、02 必須綠；DB 只存 admitted machine digest／transition tuple，不能自己補 edge。
- 【推論】只有 state-owner composition root 能 import concrete sqlite adapter；CLI、HTTP、UI、backend、evaluator 不得取得 DB path。
- 【推論】event payload inline hard max 64 KiB；大 bytes 在 Plan 04 先進 CAS，再只記 digest。
- 【推論】single transaction event hard max 32；所有 committed event 有 global_seq、aggregate_seq、causation／correlation、reason與 schema digest。
- 【推論】lease 30 秒、每 10 秒 renew；takeover 才增 fencing epoch，舊 epoch 永遠不能 commit。

## 子系統規格

【推論】state owner API 接 `CommandEnvelope(command_id, entity_id, expected_seq, trigger_id, payload)`，載入 entity 已釘 MachinePlan，取得唯一 transition，再在一筆 transaction 寫 event＋head。command idempotency key 重送回同 result，不重跑 decision。

【推論】tail publisher 的 truth 是 state-owner read port，不是直接另開權威 SQLite reader；它按 cursor 取 committed canonical event bytes，寫 WAL cache並更新 durable publisher cursor。UI subscriber只拿 range／live event port。

## File Structure

```text
規格/語言/事件.schema.json                    — event tagged-union envelope。
規格/儲存/保證/權威交易崩潰重建.claim.json     — state-owner persisted-only restart。
規格/介面/保證/事件流獨立.claim.json           — reader 不碰 owner transaction。
nova/核心/事件.py                              — EventEnvelope value／canonical bytes。
nova/應用/
├── 工作單元.py                                — state-owner port，不暴露 SQL。
└── test_工作單元.py                           — fake/real contract。
nova/基礎設施/狀態庫/sqlite/
├── 擁有者.py                                  — one connection command loop／IPC。
├── 工作單元.py                                — BEGIN／append／head／commit。
├── 機器目錄.py                                — admitted transition catalog。
├── 租約.py                                    — lease／renew／takeover fencing。
├── 讀取埠.py                                  — publisher/recovery bounded range port。
├── test_工作單元.py                           — adapter contract。
└── 遷移/
    ├── 0001_事件與機器目錄.sql                — journal、catalog、head、idempotency。
    └── 0002_租約.sql                          — lease owner／expiry／epoch。
nova/基礎設施/事件流/sqlite/
├── 發布器.py                                  — global_seq idempotent copy。
├── 尾隨庫.py                                  — WAL range/read-only subscription。
├── test_發布器.py                             — duplicate／gap／restart。
└── 遷移/0001_尾隨事件.sql                     — event bytes、digest、recorded_at、cursor。
nova/啟動/狀態擁有者.py                        — 唯一 state DB composition root。
架構/
├── 依賴規則.toml                              — sqlite import allowlist。
├── 檢查後端依賴.py                            — AST import graph checker。
└── test_依賴規則.py                           — direct DB bypass fixture。
驗收/儲存/
├── 測_非法轉移資料庫負控.py                   — composite FK 第二道紅。
├── 測_強制終止矩陣.py                         — boundary SIGKILL。
├── 測_租約回收.py                             — fencing epoch。
└── 測_事件流獨立.py                           — long tail 不持 owner transaction。
```

## Dependency Gate

前置計畫：01 02

【推論】Plan 01 提供 executable claims；Plan 02 提供 MachinePlan、transition catalog rows與 illegal-edge decision。缺 02 時 SQLite 只能存自由 state string，DB 負控沒有 oracle。

---

### Task 1: 固定事件 envelope 與 canonical bytes

**Files:**
- Create: `規格/語言/事件.schema.json`
- Create: `nova/核心/事件.py`
- Create: `nova/應用/工作單元.py`
- Create: `nova/應用/test_工作單元.py`

**Interfaces:**
- Produces: `EventEnvelope.to_canonical_bytes() -> bytes`
- Produces: `StateUnitOfWork.execute(CommandEnvelope) -> CommandResult`

**ClaimSpec:** 【推論】`event.envelope.causal-and-canonical` 從紅轉綠。

**固定負控:** 【推論】TRANSITION 缺 machine digest、aggregate_seq 跳號、inline payload 65,537 bytes 都在進 DB 前 direct red。
`missing-causal-fields`：缺 causation／correlation／reason／schema digest 任一的 event，
必須紅在 `envelope_requires_causal_fields`——Global Constraints 要求所有 committed event
帶這四欄、claim 名為 causal-and-canonical，causal 半邊要有自己的殺手。
（R9 覆蓋審修正：原 envelope fence 漏列四欄且 causal 零負控。）

- [ ] **Step 1: 寫 event schema 負控 tests**

```python
def test_transition_缺_machine_digest_拒絕(transition_event: dict[str, object]) -> None:
    del transition_event["machine_digest"]
    assert validate_event(transition_event).code == "MISSING_TRANSITION_FIELD"
```

- [ ] **Step 2: 跑紅測**

Run: `uv run pytest -q nova/應用/test_工作單元.py -k event`

Expected: 【推論】FAIL with missing EventEnvelope。

- [ ] **Step 3: 寫 frozen envelope 與 payload guard**

```python
@dataclass(frozen=True, slots=True)
class EventEnvelope:
    event_id: UUID
    global_seq: int
    event_kind: EventKind
    aggregate_id: UUID
    aggregate_seq: int
    causation_id: UUID
    correlation_id: UUID
    reason_code: SemanticId
    schema_digest: Sha256Ref
    payload_digest: Sha256Ref
    canonical_payload: bytes

    def require_size(self) -> None:
        if len(self.canonical_payload) > 65_536:
            raise EventPayloadTooLarge(len(self.canonical_payload))
```

- [ ] **Step 4: 跑 envelope tests**

Run: `uv run pytest -q nova/應用/test_工作單元.py -k event`

Expected: 【推論】PASS。

- [ ] **Step 5: 跑 canonical bytes designated mutation**

Run: `uv run python 工具/跑驗收.py --claim event.envelope.causal-and-canonical`

Expected: 【推論】拿掉 sort_keys 的 subject direct red。

- [ ] **Step 6: Commit**

```bash
git add 規格/語言/事件.schema.json nova/核心/事件.py nova/應用/工作單元.py nova/應用/test_工作單元.py
git commit -m "feat: 定義 canonical 的領域事件封套"
```

---

### Task 2: 建 SQLite migration 與 declared-transition composite FK

**Files:**
- Create: `nova/基礎設施/狀態庫/sqlite/遷移/0001_事件與機器目錄.sql`
- Create: `nova/基礎設施/狀態庫/sqlite/機器目錄.py`
- Create: `nova/基礎設施/狀態庫/sqlite/test_工作單元.py`
- Create: `驗收/儲存/測_非法轉移資料庫負控.py`

**Interfaces:**
- Produces: `MachineCatalogStore.admit(MachinePlan) -> MachineDigest`
- DB FK key: `(machine_digest, transition_id, from_state, to_state)`。

**ClaimSpec:** 【推論】`storage.transition.foreign-key-declared-only` 從紅轉綠。

**固定負控:** 【推論】application 直接 insert `ready→secret-pass` 即使關掉 Python transition guard，也必須 SQLite constraint red。
NULL 語意五格（CHECK 綁 `event_kind`，不是裸的全有全無）：
`partial-null-transition-row`：TRANSITION 列 `machine_digest` 設值、`transition_id` 留
NULL 直插，必須 IntegrityError——**SQLite composite FK 一律按 `MATCH SIMPLE`，
任一欄 NULL 即整條不檢查**（官方文件：不執行 `MATCH FULL`）。
`all-null-transition-row`：TRANSITION 列四欄**全 NULL** 直插，必須 IntegrityError——
裸的「全有全無」CHECK 仍放行這條，繞過 FK 的孔沒補完；綁 `event_kind` 才封死。
`non-transition-smuggles-tuple`：非 TRANSITION 列偷帶完整 transition tuple，
必須 IntegrityError——反向也封。
防恆真兩格：合法 TRANSITION 列（四欄齊、tuple 在 admitted_transition）照常提交；
合法非 TRANSITION 列（四欄全 NULL）照常提交。
（R10 重做：R9-07 的裸 CHECK 驗了問題沒驗修法——「驗問題不等於驗修法」。）

- [ ] **Step 1: 寫直接 SQL bypass test**

```python
def test_非法_tuple_無法提交(db: sqlite3.Connection, admitted_machine: MachinePlan) -> None:
    admit_machine(db, admitted_machine)
    with pytest.raises(sqlite3.IntegrityError):
        insert_transition_event(db, machine=admitted_machine.digest, transition="secret", from_state="ready", to_state="secret-pass")
```

- [ ] **Step 2: 跑紅測**

Run: `uv run pytest -q 驗收/儲存/測_非法轉移資料庫負控.py`

Expected: 【推論】FAIL because illegal row commits or tables absent。

- [ ] **Step 3: 寫 migration constraints**

```sql
CREATE TABLE admitted_transition (
  machine_digest BLOB NOT NULL,
  transition_id TEXT NOT NULL,
  from_state TEXT NOT NULL,
  to_state TEXT NOT NULL,
  PRIMARY KEY (machine_digest, transition_id, from_state, to_state)
) STRICT;

CREATE TABLE event_journal (
  global_seq INTEGER PRIMARY KEY,
  event_kind TEXT NOT NULL,
  machine_digest BLOB,
  transition_id TEXT,
  from_state TEXT,
  to_state TEXT,
  event_bytes BLOB NOT NULL,
  event_digest BLOB NOT NULL,
  FOREIGN KEY (machine_digest, transition_id, from_state, to_state)
    REFERENCES admitted_transition(machine_digest, transition_id, from_state, to_state),
  CHECK (
    (event_kind = 'TRANSITION' AND machine_digest IS NOT NULL AND transition_id IS NOT NULL
     AND from_state IS NOT NULL AND to_state IS NOT NULL)
    OR
    (event_kind <> 'TRANSITION' AND machine_digest IS NULL AND transition_id IS NULL
     AND from_state IS NULL AND to_state IS NULL)
  )
) STRICT;
```

- [ ] **Step 4: 跑 DB FK tests**

Run: `uv run pytest -q nova/基礎設施/狀態庫/sqlite/test_工作單元.py 驗收/儲存/測_非法轉移資料庫負控.py`

Expected: 【推論】PASS with `PRAGMA foreign_keys=ON` verified。

- [ ] **Step 5: 跑 named mutation：移除 FOREIGN KEY**

Run: `uv run python 工具/跑驗收.py --claim storage.transition.foreign-key-declared-only`

Expected: 【推論】direct red `illegal_tuple_committed`。

- [ ] **Step 6: Commit**

```bash
git add nova/基礎設施/狀態庫/sqlite/遷移/0001_事件與機器目錄.sql nova/基礎設施/狀態庫/sqlite/機器目錄.py nova/基礎設施/狀態庫/sqlite/test_工作單元.py 驗收/儲存/測_非法轉移資料庫負控.py
git commit -m "feat: 在 SQLite 裡強制宣告過的轉移"
```

---

### Task 3: 建 single-owner command loop 與唯一 writer connection

**Files:**
- Create: `nova/基礎設施/狀態庫/sqlite/擁有者.py`
- Create: `nova/啟動/狀態擁有者.py`
- Modify: `nova/基礎設施/狀態庫/sqlite/test_工作單元.py`

**Interfaces:**
- Produces: `StateOwnerClient.execute(CommandEnvelope) -> CommandResult`
- Produces: owner startup attestation `pid/db_path/journal_mode/synchronous`。

**ClaimSpec:** 【推論】`storage.state-owner.single-writer-topology` 從紅轉綠。

**固定負控:** 【推論】第二個 owner 對相同 data root 啟動必須 `STATE_OWNER_ALREADY_ACTIVE`；worker 直接拿 DB path 的 fixture 被依賴／startup gate 拒絕。

- [ ] **Step 1: 寫 two-owner test**

```python
def test_同資料根只能一個_owner(tmp_path: Path) -> None:
    first = start_owner(tmp_path)
    second = start_owner(tmp_path)
    assert first.ready
    assert second.failure.code == "STATE_OWNER_ALREADY_ACTIVE"
```

- [ ] **Step 2: 跑紅測**

Run: `uv run pytest -q nova/基礎設施/狀態庫/sqlite/test_工作單元.py -k owner`

Expected: 【推論】FAIL with missing owner process。

- [ ] **Step 3: 寫 lockfile attestation、one connection與 command queue**

```python
def open_authoritative_connection(path: Path) -> sqlite3.Connection:
    connection = sqlite3.connect(path, isolation_level=None)
    connection.execute("PRAGMA journal_mode=DELETE")
    connection.execute("PRAGMA synchronous=FULL")
    connection.execute("PRAGMA foreign_keys=ON")
    return connection
```

- [ ] **Step 4: 跑 single-owner tests**

Run: `uv run pytest -q nova/基礎設施/狀態庫/sqlite/test_工作單元.py -k owner`

Expected: 【推論】PASS；worker client 沒有 connection property。

- [ ] **Step 5: 驗 startup attestation**

Run: `uv run python -m nova.啟動.狀態擁有者 --data-root /private/tmp/nova-owner-probe --一次性自檢`

Expected: 【推論】exit 0 and `journal_mode=delete,synchronous=2,connections=1`；自檢清掉 probe root。

- [ ] **Step 6: Commit**

```bash
git add nova/基礎設施/狀態庫/sqlite/擁有者.py nova/啟動/狀態擁有者.py nova/基礎設施/狀態庫/sqlite/test_工作單元.py
git commit -m "feat: 讓命令經由單一狀態 owner 串行化"
```

---

### Task 4: 原子 append event、head 與 command idempotency

**Files:**
- Create: `nova/基礎設施/狀態庫/sqlite/工作單元.py`
- Modify: `nova/基礎設施/狀態庫/sqlite/遷移/0001_事件與機器目錄.sql`
- Modify: `nova/基礎設施/狀態庫/sqlite/test_工作單元.py`

**Interfaces:**
- Consumes: `MachinePlan.transition(current_state, trigger, facts)` from Plan 02。
- Produces: exactly one stored `CommandResult` per command id；event batch 1–32。

**ClaimSpec:** 【推論】`storage.command.event-head-atomic`、`storage.command.idempotent-replay` 從紅轉綠。

**固定負控:** 【推論】event insert 後、head update 前 raise；相同 command 重送；33-event command，各自不得留下 partial rows。

- [ ] **Step 1: 寫 failure injection tests**

```python
def test_event_後_head_前失敗整筆_rollback(uow: SqliteUnitOfWork) -> None:
    result = uow.execute(command(), fault=FaultPoint.AFTER_EVENT_BEFORE_HEAD)
    assert result.failure.code == "INJECTED_STORAGE_FAULT"
    assert uow.count_events() == 0
    assert uow.load_head(entity_id()) is None
```

- [ ] **Step 2: 跑紅測**

Run: `uv run pytest -q nova/基礎設施/狀態庫/sqlite/test_工作單元.py -k 'atomic or idempotent or fanout'`

Expected: 【推論】FAIL due partial state or missing UoW。

- [ ] **Step 3: 寫 transaction pipeline**

```python
def execute(self, command: CommandEnvelope) -> CommandResult:
    with self.immediate_transaction():
        if cached := self.command_result(command.command_id):
            return cached
        head = self.require_expected_head(command.entity_id, command.expected_seq)
        transition = self.machine_engine.decide(head, command.trigger)
        events = self.event_factory.build(transition, command)
        require_range("events_per_transaction", len(events), 1, 32)
        self.append_events(events)
        self.update_head(head.fold(events))
        return self.store_command_result(command.command_id, events)
```

- [ ] **Step 4: 跑 atomic／idempotency tests**

Run: `uv run pytest -q nova/基礎設施/狀態庫/sqlite/test_工作單元.py -k 'atomic or idempotent or fanout'`

Expected: 【推論】PASS；重送回相同 event ids，不增加 rows。

- [ ] **Step 5: 跑 named mutation：commit 移到 head 前**

Run: `uv run python 工具/跑驗收.py --claim storage.command.event-head-atomic`

Expected: 【推論】direct red `partial_commit_visible`。

- [ ] **Step 6: Commit**

```bash
git add nova/基礎設施/狀態庫/sqlite/工作單元.py nova/基礎設施/狀態庫/sqlite/遷移/0001_事件與機器目錄.sql nova/基礎設施/狀態庫/sqlite/test_工作單元.py
git commit -m "feat: 事件與 head 原子提交"
```

---

### Task 5: 建 lease renew／takeover fencing

**Files:**
- Create: `nova/基礎設施/狀態庫/sqlite/遷移/0002_租約.sql`
- Create: `nova/基礎設施/狀態庫/sqlite/租約.py`
- Create: `驗收/儲存/測_租約回收.py`

**Interfaces:**
- Produces: `claim_lease(entity, owner, now) -> LeaseToken(epoch, expires_at)`
- Produces: `renew(token, now) -> LeaseToken` without epoch change。
- Produces: `takeover(expired, new_owner, now) -> epoch+1`。

**ClaimSpec:** 【推論】`storage.lease.fencing-single-valid-owner` 從紅轉綠。

**固定負控:** 【推論】舊 epoch 在 takeover 後 commit、renew 自增 epoch、未過期 takeover 都必須紅。

- [ ] **Step 1: 寫 fake-time fencing tests**

```python
def test_takeover_後舊_token_不能_commit(lease_store: LeaseStore, clock: FakeClock) -> None:
    old = lease_store.claim(entity_id(), "worker-a", clock.now())
    clock.advance(seconds=31)
    lease_store.takeover(entity_id(), "worker-b", clock.now())
    assert lease_store.authorize(old).code == "STALE_FENCING_EPOCH"
```

- [ ] **Step 2: 跑紅測**

Run: `uv run pytest -q 驗收/儲存/測_租約回收.py`

Expected: 【推論】FAIL with missing lease store。

- [ ] **Step 3: 寫 SQL CAS 與 time policy**

```python
LEASE_SECONDS = 30
RENEW_SECONDS = 10

def renew(self, token: LeaseToken, now: Instant) -> LeaseToken:
    changed = self.db.execute(RENEW_SQL, (now.plus_seconds(LEASE_SECONDS), token.entity_id, token.owner_id, token.epoch)).rowcount
    if changed != 1:
        raise StaleFencingEpoch(token)
    return replace(token, expires_at=now.plus_seconds(LEASE_SECONDS))
```

- [ ] **Step 4: 跑 lease matrix**

Run: `uv run pytest -q 驗收/儲存/測_租約回收.py`

Expected: 【推論】PASS。

- [ ] **Step 5: SIGSTOP 20／31 秒 cases**

Run: `uv run pytest -q 驗收/儲存/測_租約回收.py -k process_pause`

Expected: 【推論】20 秒可 renew；31 秒 takeover，舊 worker fenced。

- [ ] **Step 6: Commit**

```bash
git add nova/基礎設施/狀態庫/sqlite/遷移/0002_租約.sql nova/基礎設施/狀態庫/sqlite/租約.py 驗收/儲存/測_租約回收.py
git commit -m "feat: 隔離過期的 worker lease"
```

---

### Task 6: 發布完全相同 event bytes 到可重建 WAL tail

**Files:**
- Create: `nova/基礎設施/狀態庫/sqlite/讀取埠.py`
- Create: `nova/基礎設施/事件流/sqlite/遷移/0001_尾隨事件.sql`
- Create: `nova/基礎設施/事件流/sqlite/尾隨庫.py`
- Create: `nova/基礎設施/事件流/sqlite/發布器.py`
- Create: `nova/基礎設施/事件流/sqlite/test_發布器.py`
- Create: `驗收/儲存/測_事件流獨立.py`

**Interfaces:**
- Produces: `read_committed_after(seq, limit) -> EventBatch`
- Produces: `TailStore.range(after, limit) -> tuple[bytes, ...]`
- Publisher SLO metadata tracks last durable cursor。

**ClaimSpec:** 【推論】`event.tail.byte-identical-idempotent` 從紅轉綠。

**固定負控:** 【推論】publisher 重啟重送同 seq、改寫 recorded_at、跳過一個 seq 都必須 red；duplicate transport可接受但 stored row 唯一。

- [ ] **Step 1: 寫 duplicate／gap／byte drift tests**

```python
def test_publisher_不得重編_event(owner: OwnerReadPort, tail: TailStore) -> None:
    publish_once(owner, tail)
    assert tail.get(1).event_bytes == owner.get(1).event_bytes
    assert tail.get(1).event_digest == owner.get(1).event_digest
```

- [ ] **Step 2: 跑紅測**

Run: `uv run pytest -q nova/基礎設施/事件流/sqlite/test_發布器.py`

Expected: 【推論】FAIL with missing tail store。

- [ ] **Step 3: 寫 WAL cache、UNIQUE seq 與 cursor transaction**

```python
def publish_batch(source: OwnerReadPort, tail: TailStore, after: int) -> int:
    batch = source.read_committed_after(after, limit=256)
    return tail.append_idempotent(batch.events, expected_after=after)
```

- [ ] **Step 4: 跑 publisher tests**

Run: `uv run pytest -q nova/基礎設施/事件流/sqlite/test_發布器.py`

Expected: 【推論】PASS；刪 tail DB 後可從 seq 0 重建。

- [ ] **Step 5: 驗 read-only tailer 不持 owner transaction**

Run: `uv run pytest -q 驗收/儲存/測_事件流獨立.py`

Expected: 【推論】250 ms tail read 期間 owner command仍在 deadline 內提交。

- [ ] **Step 6: Commit**

```bash
git add nova/基礎設施/狀態庫/sqlite/讀取埠.py nova/基礎設施/事件流/sqlite
git commit -m "feat: 發佈逐位元組相同的事件尾流"
```

---

### Task 7: 跑 transaction-boundary SIGKILL recovery matrix

**Files:**
- Create: `規格/儲存/保證/權威交易崩潰重建.claim.json`
- Create: `驗收/儲存/測_強制終止矩陣.py`
- Modify: `nova/基礎設施/狀態庫/sqlite/擁有者.py`
- Modify: `nova/基礎設施/事件流/sqlite/發布器.py`

**Interfaces:**
- Produces: fault-point protocol `BEFORE_BEGIN | AFTER_EVENT | AFTER_HEAD | AFTER_COMMIT | AFTER_TAIL_ROW | AFTER_TAIL_CURSOR`。
- Recovery report: committed seq、head seq、publisher cursor、lease epochs。

**ClaimSpec:** 【推論】`storage.state-owner.crash-recovery`、`event.publisher.crash-resumable` 從紅轉綠；Work-level recovery由plan 09另行擁有。

**固定負控:** 【推論】把 publisher cursor 先於 tail row commit、head 單獨 commit、重啟信任 memory cache 三個 subjects 必須 direct red。

- [ ] **Step 1: 寫 parameterized subprocess kill matrix**

```python
@pytest.mark.parametrize("fault", tuple(FaultPoint))
def test_sigkill_後只靠_data_root_恢復(fault: FaultPoint, tmp_path: Path) -> None:
    run_owner_until_fault(tmp_path, fault)
    recovered = restart_from_data_root(tmp_path)
    assert recovered.heads_equal_event_fold
    assert recovered.tail_is_prefix_of_authoritative_log
    assert recovered.valid_lease_count_per_entity <= 1
```

- [ ] **Step 2: 跑矩陣確認至少一個 crash gap 失敗**

Run: `uv run pytest -q 驗收/儲存/測_強制終止矩陣.py -x`

Expected: 【推論】FAIL with a specific inconsistent cursor/head witness。

- [ ] **Step 3: 補 startup recovery scan 與 cursor reconciliation**

```python
def recover(owner: StateStore, tail: TailStore) -> RecoveryReport:
    owner.verify_heads_against_suffix()
    tail.truncate_after(owner.max_global_seq())
    tail.resume_publisher_from(tail.last_contiguous_seq())
    owner.reclaim_expired_leases()
    return RecoveryReport.capture(owner, tail)
```

- [ ] **Step 4: 跑完整 kill matrix**

Run: `uv run pytest -q 驗收/儲存/測_強制終止矩陣.py`

Expected: 【推論】PASS；每個 fault point 都有 before／after evidence。

- [ ] **Step 5: 重複 20 次抓 race（重複由測試內 parametrize 承載，不引外部 plugin）**

```python
@pytest.mark.parametrize("回合", range(20))
@pytest.mark.parametrize("fault", tuple(FaultPoint))
def test_sigkill_矩陣重複抓競態(回合: int, fault: FaultPoint, tmp_path: Path) -> None:
    ...
```

Run: `uv run pytest -q 驗收/儲存/測_強制終止矩陣.py -k 重複`

Expected: 【推論】PASS；無 flaky gap。20 次只值一個 parametrize，不值一個工具鏈依賴。

- [ ] **Step 6: Commit**

```bash
git add 規格/儲存/保證/權威交易崩潰重建.claim.json 驗收/儲存/測_強制終止矩陣.py nova/基礎設施/狀態庫/sqlite/擁有者.py nova/基礎設施/事件流/sqlite/發布器.py
git commit -m "test: 證明狀態與尾流的當機恢復"
```

---

### Task 8: 機械禁止 DB bypass

**Files:**
- Create: `架構/依賴規則.toml`
- Create: `架構/檢查後端依賴.py`
- Create: `架構/test_依賴規則.py`
- Create: `規格/介面/保證/事件流獨立.claim.json`

**Interfaces:**
- Produces: `check_import_graph(root) -> tuple[ArchitectureViolation, ...]`
- Allow concrete sqlite imports only under `nova.基礎設施` and `nova.啟動` composition roots。
- Resolves: static imports plus literal `importlib.import_module("...")`／`__import__("...")` aliases into the same graph；reflective or nonliteral import target is forbidden rather than ignored。

**ClaimSpec:** 【推論】`architecture.state-store.no-bypass`、`view.event-stream.owner-independent` 從紅轉綠。

**固定負控:** 【實測】fixture `nova/介面/HTTP/壞查詢.py`以static import違規時必須red；同一條邊改成alias後的`importlib.import_module("nova.基礎設施.狀態庫.sqlite.工作單元")`或`__import__(...)`仍必須red。`getattr(importlib_alias, "import_module")`、字串拼接／變數產生module name無法可靠解析，production一律`DYNAMIC_IMPORT_UNVERIFIABLE`，不得因掃描不到而全綠。

- [ ] **Step 1: 寫 illegal import fixture test**

```python
def test_HTTP_不能_import_狀態庫(tmp_path: Path) -> None:
    write_fixture(tmp_path, "nova/介面/HTTP/壞查詢.py", "from nova.基礎設施.狀態庫.sqlite import 工作單元\n")
    assert check_import_graph(tmp_path)[0].code == "FORBIDDEN_IMPORT"

@pytest.mark.parametrize("source", [
    'import importlib as i\ni.import_module("nova.基礎設施.狀態庫.sqlite.工作單元")',
    '__import__("nova.基礎設施.狀態庫.sqlite.工作單元")',
    'getattr(__import__("importlib"), "import_module")("nova." + TARGET)',
])
def test_動態_import_不能藏違規邊(tmp_path: Path, source: str) -> None:
    write_fixture(tmp_path, "nova/介面/HTTP/壞查詢.py", source)
    assert check_import_graph(tmp_path)
```

- [ ] **Step 2: 跑紅測**

Run: `uv run pytest -q 架構/test_依賴規則.py`

Expected: 【推論】FAIL with missing checker。

- [ ] **Step 3: 寫AST static/literal-dynamic import graph checker與fail-closed allowlist**

```toml
[[deny]]
from = ["nova.領域.*", "nova.權威.*", "nova.應用.*", "nova.介面.*", "nova.介接.*"]
to = ["sqlite3", "nova.基礎設施.狀態庫.*"]

[[allow]]
from = ["nova.基礎設施.*", "nova.啟動.*"]
to = ["sqlite3", "nova.基礎設施.狀態庫.*"]
```

【推論】AST先追蹤`importlib`與`import_module` aliases；literal call轉成普通edge再套deny/allow。`__import__`同理。任何module argument不是單一string literal、以`getattr`取得import API、覆寫alias、或從`builtins`反射取得`__import__`，直接拒絕整個production module。合法plugin選擇必須用封閉registry把semantic id映到靜態imported adapter，不准以runtime字串當module address。

- [ ] **Step 4: 跑 dependency tests**

Run: `uv run pytest -q 架構/test_依賴規則.py`

Expected: 【推論】PASS；static、literal dynamic與unverifiable reflective三組fixed negatives都被指定reason抓住。

- [ ] **Step 5: 掃 production source tree**

Run: `uv run python 架構/檢查後端依賴.py nova`

Expected: 【推論】exit 0；fixed illegal fixture direct red evidence另存。

- [ ] **Step 6: Commit**

```bash
git add 架構 規格/介面/保證/事件流獨立.claim.json
git commit -m "build: 禁止繞過權威狀態庫"
```

## Plan Exit Gate

```bash
uv run pytest -q -n 2 nova/應用 nova/基礎設施/狀態庫 nova/基礎設施/事件流 驗收/儲存 架構 && uv run python 架構/檢查後端依賴.py nova
```

【推論】exit gate 必須證明：非法 transition Python 與 DB 兩道都紅；每個 SIGKILL fault point重啟一致；tail 可刪重建；第二 owner／舊 lease epoch／直接 DB import 都紅。

## Execution Handoff

【推論】完成後進 [04-內容證據與分層儲存.md](./04-內容證據與分層儲存.md)。Plan 04 會在這個 journal 上增加 CAS segment manifest、checkpoint、retention與 backup，不得回頭讓 archive worker取得第二條 writer connection。
