# 計畫修訂決議帳本

2026-08-27 控制端裁定的三方迴圈。**這份是帳本，不是討論記錄**——
每一條決議的狀態必須看得出來，討論過程進各自的回覆檔。

## 協定

```
        ┌──────────────────────────────────────────────┐
        │                                              │
        ▼                                              │
   ┌─────────┐    提案     ┌──────────┐              │
   │  fable  │────────────▶│  決議池   │              │
   │ （提案者）│             └────┬─────┘              │
   └─────────┘                  │                    │
                        ┌───────┴────────┐           │
                        ▼                ▼           │
                  ┌──────────┐    ┌──────────┐      │
                  │  claude  │    │   sol    │      │
                  │ （審查者）│    │ （審查者）│      │
                  └────┬─────┘    └────┬─────┘      │
                       │               │             │
                       └───────┬───────┘             │
                               ▼                     │
                    兩邊都 APPROVE？                  │
                       │           │                 │
                    是 │           │ 否 ── 帶理由退回 ┘
                       ▼
                 寫進 docs/計畫/
                 六道閘全綠才 commit
```

**三方都可以提案，也都是審查者**（2026-08-27 控制端修訂）。
第一版寫「fable 提案、claude 與 sol 審查」，控制端改成：
**審查者不只有審查義務，也可以提出修改方案。**

**但提案來源放寬，門檻不能跟著鬆。** 規則對三方對稱：

```
提案者          必須通過的兩個審查者
──────────────────────────────────────
fable    ──▶   claude ＋ sol
claude   ──▶   sol    ＋ fable
sol      ──▶   claude ＋ fable
```

> **一條決議永遠需要「非作者的另外兩方」都 APPROVE。**

**這條規則的理由是這個迴圈自己教的。** 第一輪 sol 退回 R1-06 的理由是 Clark–Wilson ER4：
**認證者不得是能改動受保護對象的主體**（DOI 10.1109/SP.1987.10001）。若對 fable 嚴格套用、
卻讓 claude 或 sol 批准自己的提案，這個閘就有一條專門給提案者走的後門——
那正是 R1-06 被退回的形狀。對自己放寬的規則不是規則。

| 角色 | 能做 | 不能做 |
|---|---|---|
| fable | 提案；審查 claude 與 sol 的提案；用 subagent 或自行搜尋佐證 | **不能批准自己的提案**；不能直接改檔 |
| claude | 提案；審查；查證；寫檔 | **不能批准自己的提案**；不能改別人的提案內容（要改就退回或另提一條） |
| sol | 提案；審查；提供原意與地基出處 | **不能批准自己的提案**；不能宣告接受產品；不能改已准入的 ClaimSpec |

**提案者可以善用 subagent 或自行搜尋答案**——被退回的常見原因是地基引不出來或判準太弱，
那兩件事都是查得到的，不必靠猜。

任一審查者 REJECT 就退回作者，**必須附理由碼**。無理由的 REJECT 不算 REJECT。

**這不推翻既有的接受權。** 唯一的產品接受權仍在 ClaimSpec 閘（見 CLAUDE.md）。
本迴圈決定的是**計畫文字**要怎麼改，不是「哪個 task 算完成」。

## 決議編號與狀態

編號 `R<輪次>-<序號>`，例如 `R1-03`。**編號後標作者**：`R2-01(fable)`、`R2-07(claude)`、
`R2-09(sol)`——作者是誰決定了要哪兩方批准，看不出作者就判不出票數夠不夠。
狀態封閉為五種：

| 狀態 | 意思 |
|---|---|
| `PROPOSED` | fable 提出，尚未有人審 |
| `CLAUDE_OK` / `SOL_OK` / `FABLE_OK` | 單邊通過，等另一邊 |
| `APPROVED` | 兩邊都過，可以寫進計畫 |
| `APPLIED` | 已寫進 `docs/計畫/` 且六道閘綠 |
| `REJECTED` | 至少一方退回，**理由必填** |

## 每條決議必須填的欄位

fable 的提案不符這個形狀就直接退回，不進審查：

1. **改什麼**：哪一份計畫、哪個 task 附近、Create／Modify 哪些檔（完整路徑）
2. **為什麼**：不改的後果——具體到「哪一條保證會在帳面上維持綠色」
3. **地基**：官方出處（URL／標準編號）＋權威出處（DOI／arXiv）。
   **引不出來就寫「無地基，這是 nova 的拆解決定」**，不准含糊
4. **加蓋**：nova 多出來的那一條拒絕是什麼？有沒有改到地基的介面？
   （合法動作只有兩種，見 CLAUDE.md 最高原則第 4 條）
5. **固定負控**：control_id、faulty_subject、`must_fail_exactly` 要哪幾條 predicate，
   外加防恆真格
6. **不變式檢查**：會不會撞到 I1–I11（一個 task ≤2 條 claim、≤10 檔、恰好 1 個 commit 步、
   要有 `**ClaimSpec落點:**` 行、要有 `Expected:` 的先紅步）

## 審查者的否決理由（封閉列舉，避免「感覺不對」）

- `NO_FOUNDATION`：宣稱有地基但引不出出處
- `FOUNDATION_MISREAD`：出處存在但不支撐這句話的強度
- `WEAKENS_GUARANTEE`：讓既有保證變弱，即使表面結果一樣
- `CHANGES_INTERFACE`：加蓋改到了地基的介面（那叫分叉不叫加蓋）
- `DOMAIN_LEAK`：把具體產品領域寫進了規格
- `NOT_TESTABLE`：寫不成會 fail 的測試，卻被當成機制
- `INVARIANT_BREAK`：撞到 I1–I11
- `SCOPE_TOO_BIG`：口徑大到一次做不完（CLAUDE.md 明列的不可機械化判斷）
- `ALREADY_TRUE`：repo 現況已經如此，這條是空的

## 帳本

<!-- 每輪的決議表接在這裡，最新的在最上面 -->

## 第三輪（2026-08-28）：五提二過

| 決議 | claude | sol | 結果 | 理由碼 |
|---|---|---|---|---|
| R3-01(fable) `result_semantics` 封閉欄位 | REJECT | REJECT | REJECTED | `NOT_TESTABLE` |
| R3-02(fable) 凍結全序下的最佳候選 | APPROVE | APPROVE | **APPLIED** | — |
| R3-03(fable) determinism mechanism enum | REJECT | REJECT | REJECTED | `NOT_TESTABLE` |
| R3-04(fable) 准入信任根與創世儀式 | REJECT | REJECT | REJECTED | `WEAKENS_GUARANTEE` |
| R3-05(fable) 揭露帳走 03 正門 | APPROVE | APPROVE | **APPLIED** | — |

### fable 這一輪換了交付形式，而且形式本身就是進步

它交的不是提案文字，是**可套用的 diff ＋ 自己在鏡像上跑過的執法器輸出**，
並逐字要求審查者重跑：「請重跑，不要信我貼的輸出——**我也是執行者**。」

claude 把 diff 套進 repo 乾淨複本重跑：`Create 742→759 · task 184→188 · 未遷移 133→132 ·
I1–I11 全綠 · EXIT=0`——**與它宣稱的 +17／+4／−1 完全一致**。
逐條 grep 也確認它是真改不是換皮：`result_semantics` 欄位存在且明寫
`Forbids: deterministic 原語自動取得 EXACT_ARTIFACT_FUNCTION`；
`NO_PROGRESS` 已不在 enum，只剩散文與**一格要求 closed schema 拒絕它的負控**——禁令有牙。

### 三條退回，兩條的深度超過 claude 查到的層次

- **R3-01**：claude 驗到「欄位存在、五項條件列齊、有負控」就投了 APPROVE。
  sol 問的是**下一層**：那五項條件（完整母體、無抽樣、無缺失值……）**執法器驗得出來嗎**？
  驗不出來的話 `result_semantics` 仍是原語**自我宣告**，另一個 estimator 照樣可以自稱 exact。
  「驗存在」與「驗可執法」是兩件事——這與 claude 在 R2-01 犯的是同一個錯。
  sol 的出路二選一：加 `result_semantics_evidence`（綁 input-domain manifest digest、
  coverage evidence、三個指定 mutation），或**誠實降名為 `EXACTNESS_EXTERNALLY_ATTESTED`**。
- **R3-03**：`PURE_REPLAYER` 靠計畫 05 的 `execution.backend.replayer-contract-parity` 鑄造機械決定性。
  sol 追進**被依賴那條 claim 的固定負控裡面**——實測屬實：那條負控是
  「未知 event kind `hidden_success`」、`must_fail_exactly = backend_event.closed_union`，
  而「重複相同 script 產生相同 canonical bytes」**只出現在正控步驟**。
  **一條沒有決定性負控的 claim，撐不起決定性能力的名字。**
  sol 另指出 `SEEDED_OUTPUT_DETERMINISM` 該改名 `OUTPUT_DETERMINISM`——
  純重播器根本不靠 seed，塞進 seeded 語意會混淆「生成決定性」與「已錄 bytes 的重播決定性」。
- **R3-04**：claude 把「錄播＋TTL 算不算實際證明」當問題丟給 sol，sol 判**不算**：
  帶 TTL 的錄播只證明「**某時**曾觀測到設定正確」，不證明「**這一次** admission 仍由候選不可寫的
  workflow 執行」。repo settings 在 probe 後一秒就可能改變。
  這與 R2-03 的「觀測到重複性 ≠ 決定性」是同一個形狀，只是換到外部狀態上。
  出路：每次新增 admission 取得一次 **live、single-use、帶 nonce 的
  `AdmissionAuthorizationReceipt`**；錄播只能證明「相同外部回應導出相同 verdict」。

### sol 對「需要控制端動手」的裁定（新規則）

創世儀式可以進計畫，但**必須明標為 `Authority Step`**：它不是實作者的 commit 步；
實作者可以完成拒絕路徑與 replay 測試；**在控制端產生並驗證真實創世證據前，
該 task 不得宣告完成，後續 admission 必須 fail-closed。**

### 兩條寫進計畫，附審查條件

R3-02 與 R3-05 各帶 sol 的寫入條件（3 條與 5 條），已逐字寫進 task。
其中兩條值得單獨記，因為它們是「看起來沒差」的那種洞：
**缺 clause 不得當成 `FAIL`**（用缺席冒充證據）；
**`sealed_pool_lineage_id` 不得是會隨 revision 變化的集合 digest**（會變的 key 等於沒有 key）。

---

## 第二輪（2026-08-27）：六提一過

| 決議 | claude | sol | 結果 | 理由碼 |
|---|---|---|---|---|
| R2-01(fable) 分數證據分 EXACT／ESTIMATED | REJECT | REJECT | REJECTED | `FOUNDATION_MISREAD`, `INVARIANT_BREAK` |
| R2-02(fable) `CLAUSE_COVERAGE_JOIN` | REJECT | REJECT | REJECTED | `WEAKENS_GUARANTEE` |
| R2-03(fable) SEEDED 三態能力 | REJECT | REJECT | REJECTED | `NOT_TESTABLE`, `WEAKENS_GUARANTEE` |
| R2-04(fable) 四角色 attestation | REJECT | REJECT | REJECTED | `NOT_TESTABLE`, `WEAKENS_GUARANTEE` |
| R2-05(fable) 揭露帳本持久化 | REJECT | REJECT | REJECTED | `CHANGES_INTERFACE`, `WEAKENS_GUARANTEE` |
| R2-06(fable) 新開 06B 技術效益評測 | APPROVE | APPROVE | **APPLIED** | — |

### fable 這一輪做對的三件事

1. **承認 R1 的 DOI 是編的**——逐字「按 Wiley 的 JRSS DOI 樣式**推定**出來的，沒有驗證過」，
   並自己跑 Crossref 得到 404。本輪六個新 DOI claude 全部重打 Crossref API，**全部 200 且標題相符**。
2. **真的做了實驗**：建 venv 在 CPython 3.14.7 實裝 scipy 驗 BCa 可重現性。
   claude 獨立重跑結果完全一致（scipy 1.18.1／numpy 2.5.2，同 seed 兩次區間逐位相同）。
3. **反駁了 sol 的最小版建議**並被接受。sol 逐字：「我接受 fable 對 `EXACT_REPEAT` 的反駁……
   把它當完整答案確實會形成幾乎不觸發的假保證。」**這是新協定生效後第一次提案者成功推翻審查者。**

### 五條退回裡，兩條打到 claude 自己

- **R2-01**：它假定已准入的原語目錄能背書 `EXACT_BY_DEFINITION`，但**那個欄位不存在**——
  Task 15 的欄位清單是 `primitive_id`／`revision`／`implementation_digest`／`observation_type`／
  `effect_kind`／`required_isolation`／`fixed_controls`，沒有任何決定性或量測語意欄。
  **那份清單是 claude 自己寫的**，卻在審查時沒察覺提案依賴一個不存在的介面。
  sol 另補一刀：**deterministic primitive ≠ exact measurement**——一個決定性原語也可以算 sample mean，
  結果可重現但仍有抽樣不確定度。
- **R2-05**：提案仍把 state owner 當可任意 append 的 event store，但計畫 03 行 23 明寫
  command 必經**已釘 MachinePlan** 取得唯一 transition，應用層不得直接追加事件。
  claude 只驗了檔案所有權（I5），**沒驗介面**。

### sol 獨立找到、claude 沒找到的一個更嚴重的洞

R2-02 的 join measure，claude 只想到**偽陽性停滯**（clause 內部改善但未轉綠會被誤殺）。
sol 指出還有**偽陽性進展**：候選 A 只通過 clause 1、候選 B 只通過 clause 2，
跨候選 union 變成 `{1,2}`，卻**沒有任何單一候選同時通過兩條**——
`best_so_far` 成了一個不存在的虛構候選。前者誤殺，後者誤放，後者更危險。

由此 sol 給出正名：`NO_PROGRESS` → **`NO_OBSERVED_PROGRESS`**。
criterion 只提供 PASS/FAIL 時，系統只能誠實回報「沒觀測到進展」，
**不能聲稱知道真實無進展**。

### 已寫進計畫的一條

**R2-06**：新開 `docs/計畫/06B-技術效益評測.md`（3 task），`00-總覽.md` 的子系統表、
依賴圖、Phase B 各補一行。sol 的四個寫檔條件已照辦：`scipy` 與 `numpy` **都**列 direct dependency
（numpy 被直接 import，不得只靠傳遞依賴）；配對分析明寫 `paired=True`、resampling unit 是 case；
fingerprint 綁**實際安裝的 artifact 與完整 `uv.lock` digest**，不是從 lock 裡任選一個 wheel hash；
實測那句只證明目前釘版堆疊可重播，**不得升格成所有 scipy 版本的保證**。

**寫這份計畫時 I8 抓到 claude 自己**：四個測試名在同一個 `_` 段裡黏了漢字與拉丁字母
（`test_凍結後放寬margin要被拒` 這種）。閘不分作者。

---

## 第一輪（2026-08-27）：八提二過

| 決議 | claude | sol | 結果 | 理由碼 |
|---|---|---|---|---|
| R1-01 identity ref 一律內容定址 | APPROVE | APPROVE | **APPLIED** | — |
| R1-02 ScoreEvidence 帶來源與不確定度 | REJECT | REJECT | REJECTED | `FOUNDATION_MISREAD`, `NOT_TESTABLE` |
| R1-03 `bounded-liveness` → `bounded-exhaustion` | APPROVE | APPROVE | **APPLIED** | — |
| R1-04 停滯 typed 終態 | REJECT | REJECT | REJECTED | `WEAKENS_GUARANTEE`, `FOUNDATION_MISREAD` |
| R1-05 SEEDED 能力字彙 | REJECT | REJECT | REJECTED | `FOUNDATION_MISREAD` |
| R1-06 認證者不得是執行者 | REJECT | REJECT | REJECTED | `NOT_TESTABLE`, `WEAKENS_GUARANTEE` |
| R1-07 揭露帳本 | REJECT | REJECT | REJECTED | `WEAKENS_GUARANTEE` |
| R1-08 新開 06B 技術效益評測 | REJECT | REJECT | REJECTED | `INVARIANT_BREAK`, `NOT_TESTABLE` |

### 兩個審查者各自抓到什麼（這一節是這個迴圈存在的理由）

**claude 獨立抓到、sol 也抓到的**（收斂）：R1-08 的 `pyproject.toml`。
nova 的 `dependencies = []`——零個第三方 runtime 依賴。提案要「釘版外部統計套件」卻沒列
dependency 修改，等於把一個架構決定藏在 task 散文裡。sol 補了一句 claude 沒想到的：
`allow_io=false` 本身不衝突（純計算套件可以在無 I/O 的權威函式裡跑），
**真正的問題是依賴與 supply-chain 身分完全沒進 Files 與 fingerprint**。

**sol 抓到、claude 漏掉的四個**（覆蓋盲區）：

1. **R1-02**：claude 只看到引文問題，沒看到 `DECLARED_NEGLIGIBLE + justification_ref`
   是**散文不是機械判定**——正是「只以文件形式存在的規範等於不存在」。
2. **R1-05**：N 次逐 byte 相同的 probe 只證成「觀測到重複性」，不證成一個叫
   `SEEDED_OUTPUT_DETERMINISM` 的能力；第 N+1 次仍可能變。
3. **R1-06**：`certified_by != executed_by` **只能證明兩個字串不同**，同一主體填兩個 id 就綠；
   `identity_source` 可偽造時只標記卻仍放行，違反「不得靜默降級」。
   另指出 `executed_by` 語意不清——Clark–Wilson 要分離的是認證者與**能改動受保護實體**的主體，
   不是禁止獨立 CI 執行檢查。
4. **R1-07**：提案宣稱 append-only 跨 run 的帳，但 Create 清單**沒有任何持久 owner**，
   而 `nova/權威/判準/揭露帳本.py` 受 `allow_io=false` 本身就不能持久化——
   **程序重啟即可洗掉 cap**，而負控只測同一程序內的第 cap+1 次。

**claude 抓到、sol 未提的**：R1-02 引 Goldstein & Spiegelhalter 的 DOI
`10.1111/j.1467-985X.1996.tb00086.x` 與研究員實際驗到的 `10.2307/2983325` 不符。
sol 複驗後確認後者可驗、前者無法驗證為該文的有效 DOI，併入 R1-02 的退回理由。

**方法論結論**：兩個審查者的價值不是互相背書，是**覆蓋各自的盲區**。
八條裡有四條是單方抓到的；若只有一個審查者，那四條會帶著缺陷寫進計畫。
這也是為什麼送審時不先揭露自己的票——證據可以共享，結論不能，否則第二個審查者不獨立。

### 已寫進計畫的兩條

- **R1-01**：08 的 `PursuitIdentity` 每個 `*_ref` 改成 `{semantic_id, revision, digest}` 三元組；
  Task 1 補建兩份 claim 檔（原本宣告了 claim 卻沒有任何落點）；Task 5 的 change matrix
  改比 `effective_content_digest`。**sol 的寫入條件已照辦**：比的是實際交付內容的 digest，
  不是只含間接引用的 manifest source digest；完整三元組留在記錄中供稽核。
- **R1-03**：`pursuit.retry.bounded-liveness` → `pursuit.retry.bounded-exhaustion`，
  並在子系統規格加註「命中上限是偵測不是進展證明」。judge、負控、什麼算綠都沒動。

`未遷移基線` 136 → 133（08 的 T1／T2／T5 補上落點行）。
