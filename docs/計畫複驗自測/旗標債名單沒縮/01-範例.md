# 範例 Implementation Plan

前置計畫：無

### Task 1: 棘輪的另一個方向

這一格驗的不是「新幽靈被抓到」（那是 `幽靈旗標` 情境），
而是**債修好了卻留在名單裡**要紅。

本情境沒有任何 `工具/` 命令，所以 i12 的「債」名單裡每一個都不再是幽靈——
執法器必須為每一個喊「棘輪只准縮」。**刪掉那個 shrink 迴圈，這格就綠**，
所以它不是恆真格。

**ClaimSpec:** 【推論】`example.ratchet-must-shrink` 從紅轉綠。

**固定負控:** 【推論】示例。

Expected: 【推論】FAIL。

```bash
git commit -m "feat: 旗標債名單沒縮"
```
