# 範例 Implementation Plan

前置計畫：無

### Task 1: 引用了本計畫不存在的 task

這一格刻意寫裸的「Task 7」——而本計畫只有一個 task。
**改號之後另一種殘留**：把 Task 19 改成 Task 20 時漏掉一處，
或反過來刪了 task 卻留著指向它的文字。

**ClaimSpec:** 【推論】`example.dangling-internal` 從紅轉綠。

**固定負控:** 【推論】示例。

Expected: 【推論】FAIL。

```bash
git commit -m "feat: 懸空計畫內引用"
```
