# 範例 Implementation Plan

前置計畫：無

### Task 1: 消費了自己沒宣告的旗標

工具本身有 Create、也宣告了一個旗標，**壞的是旗標面**：
底下那個 `--never-declared` 從來沒有出現在任何 `- Produces:` 裡。

**Files:**
- Create: `工具/範例工具.py`

**Interfaces:**
- Produces: `工具/範例工具.py --check` 回零即通過。

**ClaimSpec:** 【推論】`example.ghost-flag` 從紅轉綠。

**固定負控:** 【推論】示例。

- [ ] **Step 1: 跑工具**

Run: `uv run python 工具/範例工具.py --never-declared`

Expected: 【推論】FAIL。

```bash
git commit -m "feat: 幽靈旗標"
```
