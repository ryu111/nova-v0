# 範例 Implementation Plan

前置計畫：無

### Task 1: 引用了另一份計畫不存在的 task

這一格刻意寫「計畫 02 Task 9」——而 fixture 裡的計畫 02 只有一個 task。
**改號之後最容易留下的就是這種**：位移做了，跨計畫的引用沒跟著改。

**ClaimSpec:** 【推論】`example.dangling-cross` 從紅轉綠。

**固定負控:** 【推論】示例。

Expected: 【推論】FAIL。

```bash
git commit -m "feat: 懸空跨計畫引用"
```
