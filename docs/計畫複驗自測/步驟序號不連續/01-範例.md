# 範例 Implementation Plan

前置計畫：無

### Task 1: 步驟序號跳號

底下的 Step 從 1 直接跳到 3。**沒有任何不變式管過 Step 序號**——
I6 只管 task 標題序號與位置，I13 只管 `Task N` 的文字引用。

跳號的實害是「有人刪了一步而沒有人知道」：**刪掉的那步如果是先紅的驗證步，
整個 task 的節奏就從「先看它紅」退化成「直接寫實作」**，而帳面完全正常。

**ClaimSpec:** 【推論】`example.step-sequence` 從紅轉綠。

**固定負控:** 【推論】示例。

- [ ] **Step 1: 先寫會紅的測試**

Run: `uv run pytest -q 驗收/範例/測_範例.py`

Expected: 【推論】FAIL。

- [ ] **Step 3: Commit**

```bash
git commit -m "feat: 步驟序號不連續"
```
